# Removal Audit CS-434

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|
| `POST /v1/natal/interpretation` | public route | historical-facade | anciens clients publics | `POST /v1/theme-natal/readings` | keep-readonly | Test `test_old_public_endpoint_is_non_generative` retourne 410 et ne call pas `NatalInterpretationService.interpret`. | faible: l'endpoint reste documente en erreur controlee. |
| `/v1/users/me/natal-chart/latest?include_interpretation=true` | public route flag | historical-facade | anciens clients publics | `POST /v1/theme-natal/readings` | delete | `users.py` retourne 410 avant lecture DB ou provider; test `test_users_latest_include_interpretation_is_non_generative`. | moyen: les clients doivent utiliser la route product-action. |
| `/v1/users/me/natal-chart/interpretation` | public route | historical-facade | anciens clients publics | `POST /v1/theme-natal/readings` | delete | `users.py` retourne 410; test `test_users_interpretation_endpoint_is_non_generative`. | moyen: ancienne generation directe fermee. |
| `natal_interpretation_short` | prompt/runtime key | historical-facade | ancien short public, tests legacy, bootstrap | product-action preview readonly | delete | Retire de `PROMPT_RUNTIME_DATA`, contrats canoniques, seed 29, seed 66.20; guard `test_public_runtime_contract_excludes_deleted_natal_generator_keys`. | faible: lectures historiques restent lisibles. |
| `natal_long_free` | prompt/runtime key | historical-facade | ancien free long public, tests legacy | product-action preview readonly | delete | Retire de `PROMPT_RUNTIME_DATA`; `AIEngineAdapter` rejette la cle; `llm_orchestration` complet PASS. | faible: runtime free preview contractuel existe. |
| Basic via `natal_interpretation` | runtime branch | historical-facade | anciens tests Basic V2 legacy | `ThemeNatalBasicFullReadingRuntime` | delete | `interpretation_service.py` leve `legacy_basic_natal_generation_disabled`; `adapter.py` rejette `plan=basic` sur `natal_interpretation`; tests product-action PASS. | moyen: l'ancien service interne Basic ne genere plus. |
| `basic_natal_prompt_payload` dans DTO legacy | prompt carrier | historical-facade | ancien adapter natal | `theme_astral_provider_payload_builder` contractuel | delete | Champ retire de `NatalExecutionInput`; `gateway.py` ne mappe plus ce carrier vers `llm_astrology_input_v1`; guard architecture PASS. | faible: builder theme astral reste le seul owner. |
| Seeds bootstrap courts/basic | bootstrap seed | historical-facade | auto-heal local | product-action et premium admin uniquement | delete | `seed_29_prompts.py` ne seed plus `natal_interpretation_short`; `seed_66_20_taxonomy.py` ne publie plus free/basic legacy. | faible: auto-heal ne requiert plus active short. |
| `natal_interpretation` premium | prompt/runtime key | admin-only-non-public | premium/admin/internal historique | futur runtime contractuel premium | keep | Adapter autorise uniquement hors Basic; public routers ne l'appellent plus; allowlist classe les hits restants. | moyen: reste a reviewer comme surface premium/admin non publique. |
