# Legacy Scan After CS-434

## Scope

Post-review-fix scan evidence for the deleted public natal generator paths.

## Zero-hit scans

Command:

```powershell
rg -n 'async def _generate_free_short|use_case_key = "natal_long_free"|else "natal_interpretation_short"|use_case_key = "natal_interpretation_short"' backend/app/services/llm_generation/natal/interpretation_service.py
```

Result: PASS zero-hit.

Command:

```powershell
rg -n 'fallback_target_key="natal_interpretation_short"|fallback_target_key="natal_long_free"|natal_interpretation_short|natal_long_free' backend/app/domain/llm/prompting/catalog.py backend/app/domain/llm/configuration/canonical_use_case_registry.py backend/app/ops/llm/bootstrap/seed_29_prompts.py backend/app/ops/llm/bootstrap/seed_66_20_taxonomy.py
```

Result: PASS zero-hit.

Command:

```powershell
rg -n 'basic_natal_prompt_payload.*natal_interpretation|template_source=.*fallback_default' backend/app backend/tests
```

Result: PASS zero-hit.

Command:

```powershell
rg -n 'NatalInterpretationService\.interpret\(|NatalInterpretationService\.interpret_chart\(' backend/app/api/v1/routers/public
```

Result: PASS zero-hit.

## Remaining classified hits

The broader symbol scan still finds historical-readonly rows, admin-only remnants, fixtures,
and guard assertions for `natal_interpretation_short`, `natal_long_free`, `AstroResponse_v3`,
`variant_code`, `forceRefresh`, and `use_case_level`.

These survivors are classified in:

- `_condamad/stories/CS-434-physical-delete-active-legacy-natal-generation-paths/evidence/legacy-allowlist.md`
- `_condamad/stories/CS-434-physical-delete-active-legacy-natal-generation-paths/evidence/removal-audit.md`

No final scan hit remains in a public route call, prompt fallback owner, prompt runtime owner,
or provider-capable short/free generation branch.
