# Story classify-critical-load-scenarios: Clarifier les scenarios de charge critiques

Status: ready-for-review

## 1. Objective

Separer les scenarios de `scripts/load-test-critical.ps1` en groupes explicites.
Le smoke critique par defaut doit etre non destructif et
`privacy_delete_request` doit etre reserve a une selection explicite.

## 2. Trigger / Source

- Source type: audit
- Source reference: `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` (`SC-005`)
- Source finding: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` (`F-005`)
- Reason for change: le script melange des vagues story/ancien libelle, LLM, B2B, monitoring et privacy destructif dans une liste active difficile a predire.

## 3. Domain Boundary

This story belongs to exactly one domain:

- Domain: `scripts/load-test-critical.ps1`
- In scope:
  - Declarer un manifest de scenarios ou des fonctions dediees par groupe.
  - Retirer les marqueurs nominaux `Story 66.35` et `Legacy critical scenarios` du chemin actif.
  - Faire de `privacy_delete_request` un scenario destructif explicitement selectionne.
  - Conserver le rapport JSON/Markdown existant.
- Out of scope:
  - Changer les endpoints backend appeles.
  - Ajouter de nouveaux scenarios fonctionnels.
  - Modifier `scripts/load-test-critical-matrix.ps1` au-dela de l'appel au contrat conserve.
- Explicit non-goals:
  - Ne pas supprimer les tests privacy sans decision explicite.
  - Ne pas changer les contrats API ou OpenAPI.
  - Ne pas contourner `RG-015` sur l'ownership scripts/ops.

## 4. Operation Contract

- Operation type: converge
- Primary archetype: ownership-routing-refactor
- Archetype reason: la story route chaque scenario vers un groupe explicite au lieu d'une responsabilite critique unique et ambigue.
- Behavior change allowed: constrained
- Behavior change constraints:
  - Le groupe par defaut doit exclure les actions destructives.
  - Les scenarios existants restent disponibles via selection explicite sauf decision bloquante documentee.
  - Les rapports JSON/Markdown gardent leur forme exploitable.
- Deletion allowed: no
- Replacement allowed: no
- User decision required if: `privacy_delete_request` doit rester dans le smoke par defaut.

## 4a. Required Contracts

| Contract | Required | Reason |
|---|---:|---|
| Runtime Source of Truth | yes | La source de verite est un garde AST/fichier sur le manifest de scenarios PowerShell. |
| Baseline Snapshot | yes | Les scenarios avant/apres doivent etre compares pour prouver la conservation hors defaut destructif. |
| Ownership Routing | yes | Chaque scenario doit avoir un groupe canonique. |
| Allowlist Exception | yes | `privacy_delete_request` est une exception destructrice selectionnable explicitement. |
| Contract Shape | no | Aucun contrat API, DTO ou type frontend n'est touche. |
| Batch Migration | no | La story modifie un seul script avec groupes internes. |
| Reintroduction Guard | yes | Les marqueurs legacy/story et le delete privacy par defaut ne doivent pas revenir. |
| Persistent Evidence | yes | Les inventaires de scenarios avant/apres doivent etre conserves. |

## 4b. Runtime Source of Truth

Use when the story changes API routes, config, runtime registration, generated contracts, persistence behavior, or architecture rules.

- Primary source of truth:
  - AST guard or generated manifest-style parser for scenario groups in `scripts/load-test-critical.ps1`.
- Secondary evidence:
  - `rg -n "Story 66\\.35|Legacy critical scenarios|privacy_delete_request" scripts/load-test-critical.ps1`.
- Static scans alone are not sufficient for this story because:
  - The default scenario group must be parsed to prove `privacy_delete_request` is not selected by default.

## 4c. Baseline / Before-After Rule

Use for refactor, convergence, migration, route restructuring, API contract changes, or behavior-preserving changes.

- Baseline artifact before implementation:
  - `_condamad/stories/classify-critical-load-scenarios/scenario-baseline.txt` generated by a dry-run command or by parsing scenario keys from `scripts/load-test-critical.ps1`.
- Comparison after implementation:
  - `_condamad/stories/classify-critical-load-scenarios/scenario-after.txt` generated by the same method.
- Expected invariant:
  - Existing non-destructive scenarios remain available; destructive privacy delete is absent from the default group.

## 4d. Ownership Routing Rule

Use for boundary, namespace, service, API, core, domain, or infra refactors.

| Responsibility type | Canonical owner | Forbidden destination |
|---|---|---|
| Smoke critique non destructif | `scripts/load-test-critical.ps1` group `smoke` | Legacy/story bucket |
| Scenarios LLM | `scripts/load-test-critical.ps1` group `llm` | Default destructive smoke |
| Scenarios B2B/billing | `scripts/load-test-critical.ps1` group `b2b` | Legacy/story bucket |
| Scenario privacy destructif | `scripts/load-test-critical.ps1` group `destructive-privacy` | Default smoke |

## 4e. Allowlist / Exception Register

Use when a broad rule has allowed exceptions.

| File | Symbol / Route / Import | Reason | Expiry or permanence decision |
|---|---|---|---|
| `scripts/load-test-critical.ps1` | `privacy_delete_request` | Scenario destructif hors defaut. | Permanent avec selection explicite. |

Rules:

- no wildcard;
- no folder-wide exception;
- no implicit exception;
- every exception must be validated by test or scan.

## 4f. Contract Shape

- Contract shape: not applicable
- Reason: no API, error, payload, export, DTO, OpenAPI contract, generated client, or frontend type is affected.

## 4g. Batch Migration Plan

- Batch migration plan: not applicable
- Reason: no multi-surface or multi-consumer migration is required.

## 4h. Persistent Evidence Artifacts

Use when the story requires audit, snapshot, baseline, OpenAPI diff, migration mapping, allowlist register, or exception register evidence.

The implementation must persist these evidence artifacts:

| Artifact | Path | Purpose |
|---|---|---|
| Scenario baseline | `_condamad/stories/classify-critical-load-scenarios/scenario-baseline.txt` | Capturer les scenarios avant convergence. |
| Scenario after-scan | `_condamad/stories/classify-critical-load-scenarios/scenario-after.txt` | Prouver le groupe par defaut non destructif apres convergence. |

## 4i. Reintroduction Guard

Use when the story removes, forbids, or converges away from a legacy route, field, import, module, prefix, OpenAPI path, frontend route, or status.

The implementation must add or update an architecture guard that fails if the removed or forbidden surface is reintroduced.

The guard must check at least one deterministic source:

- forbidden symbols or states

Required forbidden examples:

- `Story 66.35`
- `Legacy critical scenarios`
- `privacy_delete_request` in default scenario group

Guard evidence:

- Evidence profile: `reintroduction_guard`; `pytest -q app/tests/unit/test_load_test_critical_script.py` verifies forbidden markers and destructive scenario routing.

## 5. Current State Evidence

The current codebase or audit indicates:

- Evidence 1: `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` - `E-009` signale des marqueurs story/legacy et `privacy_delete_request` actif.
- Evidence 2: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` -
  `F-005` recommande de separer smoke, LLM et tests destructifs privacy.
- Evidence 3: `scripts/load-test-critical.ps1` - script cible de la convergence.
- Evidence 4: `_condamad/stories/regression-guardrails.md` - invariants consultes avant cadrage, notamment `RG-015`.

## 6. Target State

After implementation:

- Les scenarios sont groupes avec une selection explicite.
- Le groupe par defaut exclut `privacy_delete_request`.
- Les rapports JSON/Markdown restent generes par le chemin existant.
- Les marqueurs story/legacy ne structurent plus le script actif.

## 6a. Regression Guardrails

- Guardrail source: `_condamad/stories/regression-guardrails.md`
- Applicable invariants:
  - `RG-015` - la story touche un script ops/perf et doit maintenir une validation d'ownership.
- Non-applicable invariants:
  - `RG-004` - aucune erreur HTTP API n'est modifiee.
  - `RG-020` - aucune taxonomie prompt LLM n'est modifiee.
- Required regression evidence:
  - Test de manifest scenarios, scan zero-hit des marqueurs, preuve que le rapport JSON/Markdown reste produit.
- Allowed differences:
  - `privacy_delete_request` quitte le groupe par defaut et devient explicite.

## 7. Acceptance Criteria

| AC | Requirement | Validation evidence required |
|---|---|---|
| AC1 | Les groupes de scenarios attendus existent. | Evidence profile: `ast_architecture_guard`; `pytest -q app/tests/unit/test_load_test_critical_script.py`. |
| AC2 | Le groupe par defaut exclut `privacy_delete_request`. | Evidence profile: `reintroduction_guard`; `pytest -q app/tests/unit/test_load_test_critical_script.py`. |
| AC3 | Les marqueurs story/legacy audites sont absents. | Evidence profile: `targeted_forbidden_symbol_scan`; `rg -n "Story 66\\.35|Legacy" scripts/load-test-critical.ps1`. |
| AC4 | Les rapports JSON/Markdown restent produits. | Evidence profile: `baseline_before_after_diff`; `pytest -q app/tests/unit/test_load_test_critical_script.py`. |
| AC5 | La story valide. | `python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py this-file`. |

## 8. Implementation Tasks

- [x] Task 1 - Structurer les groupes de scenarios (AC: AC1, AC2)
  - [x] Subtask 1.1 - Extraire les definitions dans un manifest ou des fonctions nommees.
  - [x] Subtask 1.2 - Placer `privacy_delete_request` dans `destructive-privacy`.

- [x] Task 2 - Supprimer les marqueurs legacy et garder les rapports (AC: AC3, AC4)
  - [x] Subtask 2.1 - Remplacer les commentaires story/legacy par des noms de groupe durables.
  - [x] Subtask 2.2 - Ajouter le test de generation JSON/Markdown.

- [x] Task 3 - Valider la story (AC: AC5)
  - [x] Subtask 3.1 - Executer tests et scans cibles.
  - [x] Subtask 3.2 - Executer validateurs CONDAMAD.

## 9. Mandatory Reuse / DRY Constraints

- Reuse:
  - La logique existante de generation de rapport dans `scripts/load-test-critical.ps1`.
  - `scripts/generate-performance-report.ps1` si le script l'utilise deja.
- Do not recreate:
  - Un second script de charge critique.
  - Des listes paralleles de scenarios avec les memes endpoints.
- Shared abstraction allowed only if:
  - Le manifest remplace une duplication existante dans le meme script.

## 10. No Legacy / Forbidden Paths

Forbidden unless explicitly approved:

- compatibility wrappers
- transitional aliases
- legacy imports
- duplicate active implementations
- silent fallback behavior
- root-level service when canonical namespace exists
- preserving old path through re-export

Specific forbidden symbols / paths:

- `Story 66.35`
- `Legacy critical scenarios`
- `privacy_delete_request` in the default group

## 11. Removal Classification Rules

- Removal classification: not applicable

## 12. Removal Audit Format

- Removal audit: not applicable

## 13. Canonical Ownership

| Responsibility | Canonical owner | Non-canonical surfaces |
|---|---|---|
| Critical load scenario grouping | `scripts/load-test-critical.ps1` manifest or group functions | Story-numbered comments and legacy bucket labels |

## 14. Delete-Only Rule

- Delete-only rule: not applicable

## 15. External Usage Blocker

- External usage blocker: not applicable

## 17. Generated Contract Check

- Generated contract check: not applicable
- Reason: no generated API, route manifest, schema, public contract, or generated client is affected.

## 18. Files to Inspect First

Codex must inspect before editing:

- `scripts/load-test-critical.ps1`
- `scripts/load-test-critical-matrix.ps1`
- `scripts/generate-performance-report.ps1`
- `_condamad/stories/regression-guardrails.md`

## 19. Expected Files to Modify

Likely files:

- `scripts/load-test-critical.ps1` - scenario grouping and destructive selection.

Likely tests:

- `backend/app/tests/unit/test_load_test_critical_script.py` - manifest and forbidden marker guard.

Files not expected to change:

- `backend/app/api/v1/routers` - API endpoints are out of scope.
- `frontend/src` - frontend behavior is out of scope.

## 20. Dependency Policy

- New dependencies: none
- Dependency changes allowed only if explicitly listed here with justification.

## 21. Validation Plan

Run or justify why skipped:

```powershell
.\.venv\Scripts\Activate.ps1
pytest -q app/tests/unit/test_load_test_critical_script.py
rg -n "Story 66\\.35|Legacy critical scenarios" scripts/load-test-critical.ps1
rg -n "privacy_delete_request" scripts/load-test-critical.ps1 backend/app/tests/unit/test_load_test_critical_script.py
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py _condamad/stories/classify-critical-load-scenarios/00-story.md
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_lint.py --strict _condamad/stories/classify-critical-load-scenarios/00-story.md
```

## 22. Regression Risks

- Risk: lancer par erreur un scenario destructif dans le smoke par defaut.
  - Guardrail: test de routing `privacy_delete_request`.
- Risk: perdre les rapports exploites par les runbooks perf.
  - Guardrail: test des sorties JSON/Markdown.

## 23. Dev Agent Instructions

- Implement only this story.
- Do not broaden the domain.
- Do not introduce new dependencies unless explicitly listed.
- Do not mark a task complete without validation evidence.
- If an AC cannot be satisfied, stop and record the blocker.
- Do not preserve legacy behavior for convenience.
- Ne pas modifier les endpoints testes sauf si un test existant prouve qu'un nom est deja obsolète et que le changement reste dans ce script.

## 24. References

- `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` - source candidate `SC-005`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` - details du finding `F-005`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` - preuve `E-009`.
- `_condamad/stories/regression-guardrails.md` - invariant `RG-015`.
