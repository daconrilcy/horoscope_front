# Astrology Constant Exceptions

| File | Symbol / Route / Import | Reason | Expiry or permanence decision |
|---|---|---|---|
| `backend/app/domain/prediction/public_astro_vocabulary.py` | `PublicAstroVocabulary._STAR_DATA` | Catalogue technique de quelques étoiles fixes utilisé pour événements display-only; aucune table DB canonique d'étoiles fixes n'existe dans le scope CS-181. | Permanent jusqu'à création d'un référentiel `astral_fixed_stars`; pas de wildcard dossier. |
| `backend/app/domain/prediction/public_astro_vocabulary.py` | `PublicAstroVocabulary._ASPECT_TONES` | Tonalité éditoriale courte pour rendu prediction; ne remplace pas les angles, familles, valences ou orbes DB-backed. | Permanent sauf migration produit vers profils éditoriaux d'aspects. |
| `backend/app/domain/prediction/aspect_reference.py` | `DEFAULT_ASPECT_SYSTEM` | Valeur de configuration runtime pour sélectionner la chaîne d'héritage des règles d'orbes quand le ruleset ne fournit pas `aspect_system` ou `aspect_school`; ne recrée pas un référentiel d'aspects. | Permanent tant que le ruleset ne fournit pas une clé obligatoire explicitement validée. |
| `backend/app/domain/prediction/event_detector.py` | `DEFAULT_ASPECT_SYSTEM` | Même valeur de configuration runtime que le helper partagé pour les orbes transit-to-natal; ne recrée pas un référentiel d'aspects. | Permanent tant que le ruleset ne fournit pas une clé obligatoire explicitement validée. |
| `backend/app/domain/prediction/event_detector.py` | `PLANET_BODY_TYPES` | Familles techniques acceptées par le matcher d'orbes; les classes effectives viennent des profils planètes runtime. | Permanent; garder borné à ce symbole exact. |
| `backend/app/domain/prediction/event_detector.py` | `EventDetector.CHALDEAN_ORDER` | Ordre algorithmique traditionnel des heures planétaires; non DB-backed dans le référentiel actuel. | Permanent jusqu'à décision produit de modéliser les heures planétaires en DB. |
| `backend/app/domain/prediction/event_detector.py` | `DAY_RULERS` local dans `_planetary_hour_ruler` | Correspondance algorithmique jour/seigneur pour heures planétaires, couplée à `CHALDEAN_ORDER`; non DB-backed aujourd'hui. | Permanent jusqu'à décision produit de modéliser les heures planétaires en DB. |
| `backend/app/domain/prediction/event_detector.py` | `ANGLE_TARGETS`, `LUMINARY_TARGETS`, `EXACT_EVENT_TYPES` | Taxonomie de routage d'événements exacts; ne contient pas labels, orbes, angles ou profils métier DB-backed. | Permanent, exact symbol only. |
| `backend/app/services/prediction/engine_orchestrator.py` | `_PLANET_NAME_MAP` | Mapping dérivé de `load_default_planet_catalog()`, source canonique existante; pas une liste locale recréée. | Permanent si dérivé du catalogue, interdit si remplacé par dict littéral. |
| `backend/app/services/prediction/engine_orchestrator.py` | `_ZODIAC_SIGNS` | Tuple dérivé de `ordered_sign_codes()`, source canonique existante; pas une liste locale recréée. | Permanent si dérivé du resolver zodiac, interdit si remplacé par tuple littéral. |
