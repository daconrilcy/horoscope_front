# Acceptance Traceability

| AC | Requirement | Implementation evidence | Validation evidence | Status |
|---|---|---|---|---|
| AC1 | `transit_client_projection_v1` is documented. | `docs/architecture/transit-client-projection-v1-contract.md` created as the canonical future B2C transit projection contract. | `python -B -c "from pathlib import Path; assert Path('docs/architecture/transit-client-projection-v1-contract.md').exists()"`; `rg -n "transit_client_projection_v1|free|basic|premium" docs/architecture/transit-client-projection-v1-contract.md`. | PASS |
| AC2 | Plan content boundaries are explicit. | Contract defines `free`, `basic`, `premium` content matrix and permitted client sections. | `rg -n "transit_client_projection_v1|free|basic|premium" docs/architecture/transit-client-projection-v1-contract.md`. | PASS |
| AC3 | Plan differentiation excludes debug access. | Contract states plans differ by narration, timing depth, explanatory richness and guidance, never debug access. | `rg -n "narration|richness|debug|raw runtime|trace" docs/architecture/transit-client-projection-v1-contract.md`. | PASS |
| AC4 | Degraded states are explicit. | Contract defines `proof_gate_blocked`, `data_incomplete`, `unsupported_technique`, `doctrine_limited`, `source_version_mismatch`. | `rg -n "degraded|unavailable|proof gate|non public" docs/architecture/transit-client-projection-v1-contract.md`. | PASS |
| AC5 | Proof gate blocks exposure. | Contract and projection governance keep transit client projection `blocked` / non public until proof gate validation. | `rg -n "degraded|unavailable|proof gate|non public" docs/architecture/transit-client-projection-v1-contract.md`; governance row in `docs/architecture/official-product-primitives-public-projections.md`. | PASS |
| AC6 | LLM writer boundaries are explicit. | Contract defines LLM as `rédacteur`, not calculator, proof owner or doctrine authority. | `rg -n "LLM|rédacteur|not calculator|proof owner" docs/architecture/transit-client-projection-v1-contract.md`. | PASS |
| AC7 | Raw runtime stays hidden. | Contract excludes raw runtime objects, graph traces, proof internals, debug fields and expert/admin payloads. | `rg -n "transit_client_projection_v1|TransitClientProjection|transit-client" backend/app/api frontend/src backend/migrations` returned exit 1: PASS, no matches. | PASS |
| AC8 | Public API surface stays unchanged. | No backend API route or OpenAPI schema was added. | `python -B -c "from app.main import app; assert 'transit_client_projection_v1' not in str(app.openapi())"`; `python -B -c "from app.main import app; assert all('transit' not in getattr(r, 'path', '') for r in app.routes)"`; `python -B -m pytest -q backend/tests/architecture/test_api_contract_neutrality.py --tb=short` -> 21 passed. | PASS |
| AC9 | Application source surfaces remain unchanged. | CS-281 edited documentation and capsule/evidence only; existing dirty backend app/test files were pre-existing and left untouched. | `git status --short -- backend/app frontend/src ...` recorded in `evidence/app-surface-status.txt`. | PASS |
| AC10 | Evidence artifacts are persisted. | `evidence/validation.txt`, `evidence/app-surface-status.txt`, `evidence/source-checklist.md`, generated traceability and final evidence are persisted. | `python -B -c "from pathlib import Path; assert Path('_condamad/stories/CS-281-transit-client-projection-by-plan/evidence/validation.txt').exists()"` after evidence write. | PASS |

Status values: `PENDING`, `PASS`, `PASS_WITH_LIMITATIONS`, `FAIL`, `BLOCKED`.
