# Removal Audit — CS-433

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|
| `shouldRefreshShortAfterBasicUpgrade` | symbol | historical-facade | `NatalInterpretation` | Explicit product action CTA | delete | `rg -n "shouldRefreshShortAfterBasicUpgrade" frontend/src/api/natal-chart frontend/src/features/natal-chart/NatalInterpretation.tsx` => no matches; `pnpm --dir frontend test -- natalInterpretation` PASS | Auto short generation after Basic upgrade returns |
| `use_case_level` | request field | historical-facade | old frontend interpretation POST | `action` | delete | `frontend/src/api/natal-chart/index.ts` posts `ThemeNatalReadingCommandRequest`; `natalChartApi` asserts body has no `use_case_level` | Backend receives technical LLM control |
| `variant_code` | request field | historical-facade | old frontend request/selection surface | backend product-action resolver | delete in natal API/action surface | scoped scan `frontend/src/api/natal-chart` + `NatalInterpretation.tsx` => no matches | Frontend selects output variant |
| `forceRefresh` / `force_refresh` | request field | historical-facade | old query refresh path | `action: "regenerate"` | delete | scoped scan => no matches; `natalInterpretation` asserts Basic upgrade request remains disabled | Hidden refresh |
| Technical `useCase` / generation `plan` request fields | TS request field | historical-facade | old public generation type | backend public contract | delete from product-action request type | `ThemeNatalReadingCommandRequest` exposes only `chart_id`, `action`, `persona_profile_id`, `locale`, `client_request_id`; lint PASS | TS API leaks internals |
| Old generator helper `fetchNatalInterpretation` | function | historical-facade | `useNatalInterpretation` | `requestThemeNatalReadingAction` | delete | `rg -n "fetchNatalInterpretation|/v1/natal/interpretation" frontend/src/api/natal-chart frontend/src/features/natal-chart` => no matches | Legacy POST remains active |
| `useNatalInterpretation` hook | hook | canonical-active after replacement | `NatalInterpretation`, tests | product-action query wrapper | replace-consumer | Hook now accepts `action` command options and posts `/v1/theme-natal/readings`; no legacy request fields in its type | Name could be mistaken for old generator; review implementation body |
