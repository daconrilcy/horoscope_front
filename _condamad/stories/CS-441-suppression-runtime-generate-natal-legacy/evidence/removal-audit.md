# Removal Audit - CS-441

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|
| `AIEngineAdapter.generate_natal_interpretation` | adapter method | historical-facade | Former positive tests and `NatalInterpretationService.interpret` generation branch | `ThemeNatalBasicFullReadingRuntime` and `/v1/theme-natal/readings` | delete | `rg -n "generate_natal_interpretation" backend/app` -> `PASS: no matches`; AST guard `test_natal_adapter_entry_point_is_removed` | Low: external Python consumers would fail fast; no public route/OpenAPI hit remains. |
| `NatalInterpretationService.interpret` provider branch | service branch | historical-facade | Former complete/short legacy generation tests | Readonly historical rows plus controlled `legacy_natal_generation_disabled` rejection | replace-consumer | `test_natal_legacy_service_does_not_build_runtime_input`; service unit tests assert replacement `/v1/theme-natal/readings` | Medium: historical generation calls now reject; this is the intended behavior change. |
| `NatalExecutionInput` construction in natal service | runtime input builder | dead | Former legacy provider request construction only | Contract-bound `theme_natal` runtime | delete | `rg -n "NatalExecutionInput|use_case_key\s*=\s*['\"]natal_interpretation" backend/app/services/llm_generation/natal` -> `PASS: no matches` | Low: readonly reads do not need provider payload construction. |
| Positive backend mocks of removed method | tests | dead | Former positive Basic/Free legacy generation tests | Rejection, extinction, readonly and `theme_natal` tests | replace-consumer | `rg -n "generate_natal_interpretation" backend/tests backend/app/tests` -> `PASS: no matches`; targeted pytest `64 passed` | Low: guards now fail on reintroduction instead of preserving legacy behavior. |
| Historical readonly natal rows | persisted data access | canonical-active | Public read/list/get flows | Readonly projection helpers in `interpretation_service.py` | keep | `backend/tests/integration/test_theme_natal_public_reads.py` in targeted pytest run | Low: provider execution removed; readonly projection preserved. |
| Public routes/OpenAPI | API contract | canonical-active | Public API consumers | `/v1/theme-natal/readings` for generation | keep | `route-openapi-after-check.txt`: no legacy runtime route/OpenAPI hits | Low: no new route added. |
