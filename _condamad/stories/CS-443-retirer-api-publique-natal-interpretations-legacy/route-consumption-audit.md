# Route Consumption Audit - CS-443

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|
| `POST /v1/natal/interpretation` | backend route | historical-facade | No first-party production caller remains. | `POST /v1/theme-natal/readings` | delete | Removed `natal_interpretation_router` from `backend/app/api/v1/routers/registry.py`; `evidence/routes-after.txt` and `evidence/openapi-after.json` omit the path. | External consumers now receive no public route; story forbids a 410 facade. |
| `/v1/natal/interpretations*` | backend route family | historical-facade | Former frontend history/get/delete helpers removed from nominal flow. | `POST /v1/theme-natal/readings` action states | delete | Deleted `backend/app/api/v1/routers/public/natal_interpretation.py`; production URL scan in `evidence/forbidden-scan-after.txt` is zero-hit. | Historical persisted reads are no longer public through this facade. |
| `/v1/natal/pdf-templates` | backend route | historical-facade | Frontend no longer fetches public PDF templates. | Theme-natal `download` / `preview` product actions | delete | Deleted the public router and replaced frontend PDF flow with `requestThemeNatalReadingAction` actions. | Template selection is local/default until a modern PDF contract exists. |
| Frontend list/get/delete/PDF helpers | frontend client | historical-facade | `NatalInterpretationSection`. | `requestThemeNatalReadingAction` plus no public delete action | replace-consumer | `frontend/src/api/natal-chart/index.ts` has no historical URL; `frontend/src/features/natal-chart/NatalInterpretation.tsx` no longer imports delete. | Existing history UI is limited to already supplied state; no legacy network call remains. |
| `natal_long_free -> natal_interpretation_short` public mapping | public code mapping | dead | None in bounded public roots. | `theme_natal_reading` public payload | delete | Mapping scan in `evidence/forbidden-scan-after.txt` is zero-hit for public routes/contracts/frontend surfaces. | Runtime provider internals remain out of scope per story non-goal. |
