<!-- Plan validation CS-048. -->

# CS-048 Validation Plan

Commands: targeted guards, `npm run lint`, `npm run test`, `npm run build`, scan `var(--token, literal)` on selected files.

Expected: only dynamic `--usage-progress` remains in selected files.

