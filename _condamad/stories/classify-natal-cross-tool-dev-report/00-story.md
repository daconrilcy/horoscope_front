# Story classify-natal-cross-tool-dev-report: Classer le rapport natal cross-tool comme outil dev backend

Status: ready-for-dev

## 1. Objective

Clarifier le contrat dev-only de `scripts/natal-cross-tool-report-dev.py`.
Documenter l'execution dans le venv backend, le refus CI et les imports de test.

## 2. Trigger / Source

- Source type: audit
- Source reference: `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` (`SC-007`)
- Source finding: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` (`F-007`)
- Reason for change: le script est utile mais brouille la frontiere dev/test/runtime en important des fixtures de test depuis la racine `scripts/`.

## 3. Domain Boundary

This story belongs to exactly one domain:

- Domain: `scripts/natal-cross-tool-report-dev.py`
- In scope:
  - Documenter le statut dev-only et l'execution avec venv actif.
  - Verifier ou ajouter le refus d'execution en CI.
  - Classifier les imports `app.tests.golden` et `scripts.cross_tool_report`.
  - Ajouter une garde qui empeche une consommation runtime nominale.
- Out of scope:
  - Changer les fixtures golden elles-memes.
  - Modifier l'algorithme de rapport natal.
  - Transformer le script en endpoint API ou feature frontend.
- Explicit non-goals:
  - Ne pas autoriser des imports de fixtures de test dans du runtime backend.
  - Ne pas modifier `RG-013` sur les imports entre modules de tests.
  - Ne pas ajouter de fallback silencieux hors venv.

## 4. Operation Contract

- Operation type: converge
- Primary archetype: namespace-convergence
- Archetype reason: la story converge un outil racine ambigu vers un contrat de namespace dev-only documente et garde.
- Behavior change allowed: constrained
- Behavior change constraints:
  - L'usage dev local reste possible avec venv actif.
  - L'execution CI doit rester refusee.
  - Les imports runtime ne doivent pas commencer a consommer `app.tests`.
- Deletion allowed: no
- Replacement allowed: no
- User decision required if: le script doit devenir un outil backend durable de production ou CI.

## 4a. Required Contracts

| Contract | Required | Reason |
|---|---:|---|
| Runtime Source of Truth | yes | La source de verite est un garde AST/fichier prouvant la frontiere dev-only. |
| Baseline Snapshot | yes | Les imports et points d'execution doivent etre captures avant/apres. |
| Ownership Routing | yes | Le script doit etre route vers une responsabilite dev backend explicite. |
| Allowlist Exception | no | Aucune exception runtime n'est autorisee; le dev-only est une classification, pas une allowlist. |
| Contract Shape | no | Aucun contrat API, DTO ou type frontend n'est touche. |
| Batch Migration | yes | Les surfaces script, helper backend et fixtures de test doivent etre classifiees separement. |
| Reintroduction Guard | yes | L'import test ne doit pas etre consomme par runtime ou CI nominal. |
| Persistent Evidence | yes | La classification dev-only doit rester dans un artefact ou registre versionne. |

## 4b. Runtime Source of Truth

Use when the story changes API routes, config, runtime registration, generated contracts, persistence behavior, or architecture rules.

- Primary source of truth:
  - AST guard over Python imports and CI branch in `scripts/natal-cross-tool-report-dev.py`.
- Secondary evidence:
  - `rg -n "natal-cross-tool-report-dev|app\\.tests\\.golden|scripts\\.cross_tool_report|CI" scripts backend docs _condamad`.
- Static scans alone are not sufficient for this story because:
  - The guard must distinguish dev/test usage from runtime backend imports.

## 4c. Baseline / Before-After Rule

Use for refactor, convergence, migration, route restructuring, API contract changes, or behavior-preserving changes.

- Baseline artifact before implementation:
  - `_condamad/stories/classify-natal-cross-tool-dev-report/import-baseline.txt`
  - Command: `rg -n "natal-cross-tool-report-dev|app\\.tests\\.golden|scripts\\.cross_tool_report|CI" scripts backend docs _condamad`
- Comparison after implementation:
  - `_condamad/stories/classify-natal-cross-tool-dev-report/import-after.txt` generated by the same command.
- Expected invariant:
  - Dev-only imports remain documented and blocked from CI/runtime usage.

## 4d. Ownership Routing Rule

Use for boundary, namespace, service, API, core, domain, or infra refactors.

| Responsibility type | Canonical owner | Forbidden destination |
|---|---|---|
| Dev-only cross-tool report command | `scripts/natal-cross-tool-report-dev.py` documented as dev backend tool | Backend runtime package |
| Golden fixture data | `backend/app/tests/golden` | Runtime backend services |
| Cross-tool report helper | Existing backend helper inspected before edit | Duplicate helper in root scripts |

## 4e. Allowlist / Exception Register

- Allowlist / exception register: not applicable
- Reason: no exception is allowed by this story.

## 4f. Contract Shape

- Contract shape: not applicable
- Reason: no API, error, payload, export, DTO, OpenAPI contract, generated client, or frontend type is affected.

## 4g. Batch Migration Plan

Use when the archetype or scope requires migration by independent batches.

| Batch | Old surface | Canonical surface | Consumers changed | Tests adapted | No-shim proof | Blocker condition |
|---|---|---|---|---|---|---|
| command | Root command | Dev docs | Docs | `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py` | No duplicate command | Missing docs. |
| fixture | `app.tests.golden` | Dev-only | Runtime none | `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py` | Import scan | Runtime hit. |
| helper | `scripts.cross_tool_report` | Existing helper | Docs/tests | `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py` | No duplicate | Ambiguous helper. |

## 4h. Persistent Evidence Artifacts

Use when the story requires audit, snapshot, baseline, OpenAPI diff, migration mapping, allowlist register, or exception register evidence.

The implementation must persist these evidence artifacts:

| Artifact | Path | Purpose |
|---|---|---|
| Import baseline | `_condamad/stories/classify-natal-cross-tool-dev-report/import-baseline.txt` | Capturer les imports et references avant classification. |
| Import after-scan | `_condamad/stories/classify-natal-cross-tool-dev-report/import-after.txt` | Prouver la frontiere dev-only apres implementation. |
| Dev-only contract note | `_condamad/stories/classify-natal-cross-tool-dev-report/dev-only-contract.md` | Documenter la classification et les preuves CI/venv. |

## 4i. Reintroduction Guard

Use when the story removes, forbids, or converges away from a legacy route, field, import, module, prefix, OpenAPI path, frontend route, or status.

The implementation must add or update an architecture guard that fails if the removed or forbidden surface is reintroduced.

The guard must check at least one deterministic source:

- forbidden symbols or states

Required forbidden examples:

- runtime import of `app.tests.golden`
- CI execution path that bypasses refusal
- duplicate `cross_tool_report` helper under root scripts

Guard evidence:

- Evidence profile: `reintroduction_guard`; `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py` verifies CI refusal and import boundaries.

## 5. Current State Evidence

The current codebase or audit indicates:

- Evidence 1: `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` - `E-012` signale les imports `app.tests.golden` et `scripts.cross_tool_report`.
- Evidence 2: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` - `F-007` recommande un classement dev backend ou un contrat dev-only.
- Evidence 3: `scripts/natal-cross-tool-report-dev.py` - script cible de la classification.
- Evidence 4: `_condamad/stories/regression-guardrails.md` - invariants consultes avant cadrage, notamment `RG-013` et `RG-015`.

## 6. Target State

After implementation:

- Le script est documente comme outil dev backend avec commande PowerShell et venv actif.
- Le refus CI est verifie par test.
- Les imports de fixtures sont bornes au script dev-only et ne deviennent pas runtime.
- La classification est visible dans `scripts/ownership-index.md` ou un artefact equivalent existant.

## 6a. Regression Guardrails

- Guardrail source: `_condamad/stories/regression-guardrails.md`
- Applicable invariants:
  - `RG-013` - la story touche la frontiere d'import de modules de test et doit eviter les imports runtime depuis `app.tests`.
  - `RG-015` - la story touche un script ops/dev qui doit avoir ownership et commande.
- Non-applicable invariants:
  - `RG-001` - aucune facade historique route n'est touchee.
  - `RG-018` - aucune fallback config LLM supportee n'est touchee.
- Required regression evidence:
  - Scan d'import, test CI refusal, documentation venv, classification dev-only persistante.
- Allowed differences:
  - Ajout de documentation et garde; comportement dev local conserve.

## 7. Acceptance Criteria

| AC | Requirement | Validation evidence required |
|---|---|---|
| AC1 | Le script refuse `CI=true` avec un message clair. | Evidence profile: `ast_architecture_guard`; `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py`. |
| AC2 | La commande dev documentee active le venv. | Evidence profile: `targeted_forbidden_symbol_scan`; `rg -n "natal-cross-tool-report-dev.py|Activate.ps1" README.md docs`. |
| AC3 | `app.tests.golden` reste limite au dev/test. | Evidence profile: `ast_architecture_guard`; `pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py`. |
| AC4 | Aucun helper `cross_tool_report` duplique n'est cree. | Evidence profile: `targeted_forbidden_symbol_scan`; `rg -n "cross_tool_report" scripts backend`. |
| AC5 | La story valide. | `python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py this-file`. |

## 8. Implementation Tasks

- [ ] Task 1 - Capturer et documenter le contrat dev-only (AC: AC1, AC2)
  - [ ] Subtask 1.1 - Ecrire `import-baseline.txt`.
  - [ ] Subtask 1.2 - Ajouter la doc d'execution avec activation venv.

- [ ] Task 2 - Ajouter les gardes d'import et CI (AC: AC1, AC3, AC4)
  - [ ] Subtask 2.1 - Tester le refus `CI=true`.
  - [ ] Subtask 2.2 - Tester l'absence d'usage runtime des fixtures.

- [ ] Task 3 - Finaliser les preuves et validations (AC: AC5)
  - [ ] Subtask 3.1 - Ecrire `import-after.txt` et `dev-only-contract.md`.
  - [ ] Subtask 3.2 - Executer validateurs CONDAMAD.

## 9. Mandatory Reuse / DRY Constraints

- Reuse:
  - Le helper existant `scripts.cross_tool_report` ou son proprietaire canonique apres inspection.
  - Les fixtures `app.tests.golden` uniquement dans le contexte dev/test documente.
- Do not recreate:
  - Une copie des fixtures golden.
  - Un second helper cross-tool dans `scripts/`.
- Shared abstraction allowed only if:
  - Elle existe deja et evite une duplication dans le script cible.

## 10. No Legacy / Forbidden Paths

Forbidden unless explicitly approved:

- compatibility wrappers
- transitional aliases
- legacy imports
- duplicate active implementations
- silent fallback behavior
- root-level service when canonical namespace exists
- preserving old path through re-export

Specific forbidden symbols / paths:

- runtime use of `app.tests.golden`
- duplicate `scripts/cross_tool_report*.py`
- CI bypass for `scripts/natal-cross-tool-report-dev.py`

## 11. Removal Classification Rules

- Removal classification: not applicable

## 12. Removal Audit Format

- Removal audit: not applicable

## 13. Canonical Ownership

| Responsibility | Canonical owner | Non-canonical surfaces |
|---|---|---|
| Natal cross-tool dev report command | `scripts/natal-cross-tool-report-dev.py` documented as dev-only | Runtime backend consumers |
| Golden natal fixtures | `backend/app/tests/golden` | Production services |

## 14. Delete-Only Rule

- Delete-only rule: not applicable

## 15. External Usage Blocker

- External usage blocker: not applicable

## 17. Generated Contract Check

- Generated contract check: not applicable
- Reason: no generated API, route manifest, schema, public contract, or generated client is affected.

## 18. Files to Inspect First

Codex must inspect before editing:

- `scripts/natal-cross-tool-report-dev.py`
- `backend/scripts/cross_tool_report.py`
- `backend/app/tests/golden`
- `_condamad/stories/regression-guardrails.md`

## 19. Expected Files to Modify

Likely files:

- `scripts/natal-cross-tool-report-dev.py` - CI refusal or docstring/guard refinement.
- `scripts/ownership-index.md` - dev-only classification if the registry exists.
- `_condamad/stories/classify-natal-cross-tool-dev-report/dev-only-contract.md` - persisted contract note.

Likely tests:

- `backend/app/tests/unit/test_natal_cross_tool_report_dev_script.py` - CI refusal and import boundary guard.

Files not expected to change:

- `backend/app/services` - runtime services must not consume test fixtures.
- `frontend/src` - no frontend behavior is in scope.

## 20. Dependency Policy

- New dependencies: none
- Dependency changes allowed only if explicitly listed here with justification.

## 21. Validation Plan

Run or justify why skipped:

```powershell
.\.venv\Scripts\Activate.ps1
pytest -q app/tests/unit/test_natal_cross_tool_report_dev_script.py
rg -n "app\\.tests\\.golden" backend/app scripts -g "*.py"
rg -n "natal-cross-tool-report-dev.py|Activate.ps1" README.md docs scripts/ownership-index.md
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py _condamad/stories/classify-natal-cross-tool-dev-report/00-story.md
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_lint.py --strict _condamad/stories/classify-natal-cross-tool-dev-report/00-story.md
```

## 22. Regression Risks

- Risk: un import de fixture de test devient nominal dans le runtime.
  - Guardrail: scan d'import et test de frontiere.
- Risk: le script dev-only passe en CI sans controle.
  - Guardrail: test `CI=true`.

## 23. Dev Agent Instructions

- Implement only this story.
- Do not broaden the domain.
- Do not introduce new dependencies unless explicitly listed.
- Do not mark a task complete without validation evidence.
- If an AC cannot be satisfied, stop and record the blocker.
- Do not preserve legacy behavior for convenience.
- Ne pas transformer le script en fonctionnalite runtime.

## 24. References

- `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` - source candidate `SC-007`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` - details du finding `F-007`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` - preuve `E-012`.
- `_condamad/stories/regression-guardrails.md` - invariants `RG-013` et `RG-015`.
