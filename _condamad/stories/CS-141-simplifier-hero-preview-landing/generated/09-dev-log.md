# Dev log CS-141

- Timer `window.setInterval(updateLiveState, 80)` supprime de `HeroSection.tsx`.
- Animation restante routée en CSS-only.
- Finding reviewer accepte: CTA analytics gardes par scan source seulement; test comportemental ajouté via `LandingPage.test.tsx`.
