# Dev Log

- Pre-existing dirty files observed before implementation: `.codex-artifacts/**`,
  `docs/**`, `output/**`, `backend/app/tests/unit/test_prediction_reference_repository.py`.
- These files are unrelated to the story and must not be reverted.
