# Baseline avant CS-175

- `NatalResult` exposait `planet_positions`, `houses`, `house_rulers` et `aspects`.
- Aucun contrat `SignRuntimeData` ni builder `build_sign_runtime_data` n'existait.
- Le JSON chart public conservait `planets`, `houses`, `aspects`, `angles` et `house_rulers`.
