# Baseline avant CS-176

- Aucun contrat `ChartBalanceRuntimeData` ou `ChartSignatureRuntimeData` n'existait.
- `build_chart_json` ne projetait pas `chart_balance` ni `chart_signature`.
- Les dominances existaient par surfaces locales: maisons via `HouseStrengthRuntimeData`, aspects via `DominantAspectEvaluator`.
