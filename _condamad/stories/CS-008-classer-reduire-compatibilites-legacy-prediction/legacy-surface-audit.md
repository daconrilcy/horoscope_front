# Legacy Surface Audit — CS-008

Baseline capture before implementation.

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|
| `EngineOutput` | internal DTO aggregate | `canonical-active` | `app.prediction.editorial_builder`, `PredictionPersistenceService`, persistence/regression tests | Long-term target remains `PersistablePredictionBundle` / `CoreEngineOutput`, but V2 editorial and persistence tests still exercise this DTO directly. | `keep` | `rg -n "\bEngineOutput\b" app tests` shows active production and test consumers. | Removing now would broaden the story into a V2 DTO migration. |
| `TimeBlock` in `app.prediction.schemas` | dataclass compatibility facade | `historical-facade` | One test fixture imported `app.prediction.schemas.TimeBlock`; runtime uses `V3TimeBlock` or `app.prediction.block_generator.TimeBlock`. | `V3TimeBlock` for V3 fixtures; `app.prediction.block_generator.TimeBlock` for V2 blocks. | `delete` | Baseline `rg -n "\bTimeBlock\b" app tests` showed the schemas import only in `app/tests/integration/test_v3_baselines.py`; other active uses target canonical owners. | Low, covered by guard and full backend suite. |
| `engine_output=` on `PredictionPersistenceService.save` | legacy keyword compatibility | `historical-facade` | Internal tests used `save(engine_output=...)`; production service already passes `bundle=`. | `bundle=` | `replace-consumer` | Baseline `rg -n "engine_output=" app tests` showed persistence call sites only in calibration/explainability tests; public projection `engine_output` kwargs are separate API context. | Low, guarded by AST check on `save(engine_output=...)`. |
| `categories` public prediction payload | public API field | `external-active` | Frontend type `DailyPredictionResponse.categories`, dashboard, daily horoscope page, mappers and fixtures. | Future public contract decision required; `domain_ranking` and `categories_internal` are adjacent fields, not a drop-in removal. | `keep` | `rg -n "categories" frontend/src backend/app/prediction/public_projection.py backend/app/api/v1/routers/public/predictions.py` shows active frontend consumers. | Removing without product/API decision would break clients. |
| `LLMNarrator` / `app.prediction.llm_narrator` | removed legacy runtime narrator | `dead` | No active backend runtime or nominal test consumers. | `AIEngineAdapter.generate_horoscope_narration`. | `delete` | Guard `tests/unit/prediction/test_llm_narrator_deprecation_guard.py` and scans keep zero active runtime hits. | Reintroduction guarded by RG-016/RG-017. |
