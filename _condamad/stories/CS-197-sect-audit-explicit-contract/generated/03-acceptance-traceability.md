# Acceptance Traceability

| AC | Requirement | Implementation evidence | Validation evidence | Status |
|---|---|---|---|---|
| AC1 | `SectCalculator` returns chart sect fields. | `ChartSectResult` added in `contracts.py`; `SectCalculator.calculate()` returns it with `chart_sect`, horizon position, boolean, basis and reference system. | `pytest -q backend/tests/unit/domain/astrology/test_sect_calculator.py` passed. | PASS |
| AC2 | Missing Sun or horizon rules raise explicit errors. | Existing missing Sun error preserved; missing `above_horizon` and missing `below_horizon` rules raise `ValueError`. | `test_sect_calculator_requires_sun`; parametrized `test_sect_calculator_requires_horizon_runtime_rules`; targeted suite passed. | PASS |
| AC3 | Dignity results consume one shared chart-level `ChartSectResult`; no planet recalculates chart sect. | `PlanetDignityScoringService` calculates `chart_sect` once and passes the same object to every `PlanetDignityResult`. | `pytest -q backend/tests/unit/domain/astrology/test_planet_dignity_scoring_service.py` passed. | PASS |
| AC4 | `NatalResult` exposes explicit sect without removing fields. | `NatalResult.dignity_sect` added; `PlanetDignityResult.sect` string remains. | `pytest -q backend/tests/unit/domain/astrology/test_natal_result_contract.py` passed. | PASS |
| AC5 | `build_chart_json()` serializes the precomputed `dignities.sect` object. | `_serialize_chart_sect()` serializes precomputed `dignity_sect` / `result.chart_sect` and fails explicitly if dignity results lack a sect contract; no `SectCalculator` import in projection. | `pytest -q backend/app/tests/unit/test_chart_json_builder.py`; `rg -n "SectCalculator" backend/app/services/chart/json_builder.py` returned zero hits. | PASS |
| AC6 | Chart result persistence stores the explicit sect contract. | `NatalResult.model_dump()` now includes `dignity_sect` and each dignity `chart_sect`. | `pytest -q backend/app/tests/unit/test_chart_result_service.py` passed. | PASS |
| AC7 | No local horizon constants, per-planet sect condition contract or legacy sect alias is introduced. | Existing local horizon constants in `AccidentalDignityCalculator` were removed and replaced by runtime horizon rule lookup. | Horizon constant scan returned zero hits; per-planet condition scan returned zero hits; alias scan hits are classified in `evidence/sect-contract-validation.md`. | PASS |
| AC8 | Before/after evidence documents the allowed JSON delta. | `evidence/sect-contract-before.json` and `evidence/sect-contract-after.json` added. | `rg -n "chart_sect|sun_horizon_position" _condamad/stories/CS-197-sect-audit-explicit-contract/evidence` expected hits. | PASS |
