# Story remove-root-route-removal-audit-validator: Retirer le validateur story-specifique de la racine scripts

Status: ready-for-review

## 1. Objective

Supprimer de la racine `scripts/` le validateur ponctuel
`scripts/validate_route_removal_audit.py` lorsqu'il est classifie comme `dead`.
Bloquer l'implementation si l'audit prouve qu'une commande racine reste necessaire.

## 2. Trigger / Source

- Source type: audit
- Source reference: `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` (`SC-001`)
- Source finding: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` (`F-001`)
- Reason for change: la racine `scripts/` conserve un outil de validation propre a une story livree, ce qui entretient une surface legacy hors outillage ops durable.

## 3. Domain Boundary

This story belongs to exactly one domain:

- Domain: `scripts/`
- In scope:
  - Classifier `scripts/validate_route_removal_audit.py` avec un audit de references.
  - Supprimer le fichier s'il est `dead`, ou stopper si son besoin executable reste prouve.
  - Mettre a jour les references CONDAMAD historiques qui pointent vers une commande racine supprimee.
- Out of scope:
  - Modifier le comportement de validation des routes historiques deja livre.
  - Reorganiser tout le dossier `scripts/`.
  - Decider du statut de `scripts/stripe-listen-webhook.sh`.
- Explicit non-goals:
  - Ne pas changer les invariants `RG-001` a `RG-003` sur les routes API historiques.
  - Ne pas affaiblir `RG-015` sur l'ownership docs/scripts/secrets/security/ops.
  - Ne pas creer de wrapper, alias, fallback ou re-export pour conserver l'ancien chemin.

## 4. Operation Contract

- Operation type: remove
- Primary archetype: dead-code-removal
- Archetype reason: le fichier cible est un validateur story-specifique dont l'audit indique des references limitees a une story CONDAMAD historique.
- Behavior change allowed: no
- Behavior change constraints:
  - Aucun comportement applicatif backend ou frontend ne doit changer.
  - Les artefacts historiques peuvent etre corriges uniquement pour ne plus annoncer une commande racine inexistante.
- Deletion allowed: yes
- Replacement allowed: no
- User decision required if: l'audit classe `scripts/validate_route_removal_audit.py` comme `external-active` ou `needs-user-decision`.

## 4a. Required Contracts

| Contract | Required | Reason |
|---|---:|---|
| Runtime Source of Truth | yes | La source de verite est un garde AST/fichier sur la presence du script racine interdit. |
| Baseline Snapshot | yes | Un scan avant/apres doit prouver que seuls les usages historiques attendus changent. |
| Ownership Routing | no | Aucun deplacement de responsabilite n'est autorise. |
| Allowlist Exception | no | Aucune exception n'est admise pour garder le fichier en racine si la classification est `dead`. |
| Contract Shape | no | Aucun contrat API, DTO, payload ou type frontend n'est touche. |
| Batch Migration | no | Il n'y a pas de migration par lots. |
| Reintroduction Guard | yes | Le chemin racine supprime ne doit pas revenir comme surface legacy. |
| Persistent Evidence | yes | L'audit de consommation et le scan avant/apres doivent rester dans le dossier de story. |

## 4b. Runtime Source of Truth

Use when the story changes API routes, config, runtime registration, generated contracts, persistence behavior, or architecture rules.

- Primary source of truth:
  - AST guard or filesystem guard over the repository script inventory.
- Secondary evidence:
  - `rg -n "validate_route_removal_audit.py|validate_route_removal_audit" .`
- Static scans alone are not sufficient for this story because:
  - The durable rule must fail from a deterministic guard if the root script file returns.

## 4c. Baseline / Before-After Rule

Use for refactor, convergence, migration, route restructuring, API contract changes, or behavior-preserving changes.

- Baseline artifact before implementation:
  - `_condamad/stories/remove-root-route-removal-audit-validator/reference-baseline.txt` generated by `rg -n "validate_route_removal_audit.py|validate_route_removal_audit" .`
- Comparison after implementation:
  - `_condamad/stories/remove-root-route-removal-audit-validator/reference-after.txt` generated by the same command.
- Expected invariant:
  - No production, test, docs, script, or generated contract consumer keeps the root script path after deletion.

## 4d. Ownership Routing Rule

- Ownership routing: not applicable
- Reason: no responsibility moves or boundary rules are affected.

## 4e. Allowlist / Exception Register

- Allowlist / exception register: not applicable
- Reason: no exception is allowed by this story.

## 4f. Contract Shape

- Contract shape: not applicable
- Reason: no API, error, payload, export, DTO, OpenAPI contract, generated client, or frontend type is affected.

## 4g. Batch Migration Plan

- Batch migration plan: not applicable
- Reason: no multi-surface or multi-consumer migration is required.

## 4h. Persistent Evidence Artifacts

Use when the story requires audit, snapshot, baseline, OpenAPI diff, migration mapping, allowlist register, or exception register evidence.

The implementation must persist these evidence artifacts:

| Artifact | Path | Purpose |
|---|---|---|
| Reference baseline | `_condamad/stories/remove-root-route-removal-audit-validator/reference-baseline.txt` | Prouver les references avant suppression. |
| Reference after-scan | `_condamad/stories/remove-root-route-removal-audit-validator/reference-after.txt` | Prouver l'absence de consommation nominale apres suppression. |
| Removal audit | `_condamad/stories/remove-root-route-removal-audit-validator/removal-audit.md` | Classifier le fichier avec preuve et decision. |

## 4i. Reintroduction Guard

Use when the story removes, forbids, or converges away from a legacy route, field, import, module, prefix, OpenAPI path, frontend route, or status.

The implementation must add or update an architecture guard that fails if the removed or forbidden surface is reintroduced.

The guard must check at least one deterministic source:

- forbidden symbols or states

Required forbidden examples:

- `scripts/validate_route_removal_audit.py`
- `validate_route_removal_audit.py`

Guard evidence:

- Evidence profile: `reintroduction_guard`;
  `pytest -q app/tests/unit/test_scripts_ownership.py` checks the forbidden root script.

## 5. Current State Evidence

The current codebase or audit indicates:

- Evidence 1: `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` - `F-001` classe le fichier comme legacy-surface a supprimer ou relocaliser apres confirmation.
- Evidence 2: `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` -
  `E-014` limite les references connues a une story historique.
- Evidence 3: `scripts/validate_route_removal_audit.py` - fichier racine cible de l'audit.
- Evidence 4: `_condamad/stories/regression-guardrails.md` - invariants consultes avant cadrage, notamment `RG-001` et `RG-015`.

## 6. Target State

After implementation:

- `scripts/validate_route_removal_audit.py` n'existe plus en racine si l'audit le classe `dead`.
- Les artefacts historiques ne prescrivent plus une commande racine supprimee.
- Un garde-fou detecte toute reintroduction du chemin racine.

## 6a. Regression Guardrails

- Guardrail source: `_condamad/stories/regression-guardrails.md`
- Applicable invariants:
  - `RG-001` - la story touche un artefact de validation lie a la suppression de facades historiques.
  - `RG-015` - la story modifie l'ownership d'un script ops/story-tool sous `scripts/`.
- Non-applicable invariants:
  - `RG-004` - aucune enveloppe d'erreur HTTP API n'est modifiee.
  - `RG-016` - aucun test LLM narrator n'est touche.
- Required regression evidence:
  - Scan avant/apres persistant, audit de classification, garde anti-retour du chemin racine.
- Allowed differences:
  - Suppression du fichier racine et correction des references historiques au chemin supprime.

## 7. Acceptance Criteria

| AC | Requirement | Validation evidence required |
|---|---|---|
| AC1 | Le fichier cible est classifie dans `removal-audit.md`. | Evidence profile: `external_usage_blocker`; `rg -n "validate_route_removal_audit" .`. |
| AC2 | Un item `dead` est supprime sans wrapper ni alias. | Evidence profile: `repo_wide_negative_scan`; `rg -n "validate_route_removal_audit.py" scripts backend frontend docs`. |
| AC3 | La story historique ne cite plus la commande racine. | Evidence profile: `targeted_forbidden_symbol_scan`; `rg -n "validate_route_removal_audit.py" _condamad/stories`. |
| AC4 | Un garde echoue si le chemin racine revient. | Evidence profile: `reintroduction_guard`; `pytest -q app/tests/unit/test_scripts_ownership.py`. |
| AC5 | La story valide. | `python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py this-file`. |

## 8. Implementation Tasks

- [x] Task 1 - Capturer et classifier les references existantes (AC: AC1)
  - [x] Subtask 1.1 - Ecrire `reference-baseline.txt` avec le scan cible.
  - [x] Subtask 1.2 - Renseigner `removal-audit.md` avec la table de classification.

- [x] Task 2 - Supprimer uniquement le fichier classifie `dead` (AC: AC2, AC3)
  - [x] Subtask 2.1 - Supprimer `scripts/validate_route_removal_audit.py` si aucune classification bloquante ne reste.
  - [x] Subtask 2.2 - Adapter les artefacts historiques qui citent l'ancienne commande racine.

- [x] Task 3 - Ajouter le garde anti-retour et valider (AC: AC4, AC5)
  - [x] Subtask 3.1 - Ajouter ou etendre un test d'ownership des scripts.
  - [x] Subtask 3.2 - Ecrire `reference-after.txt` et executer les validations.

## 9. Mandatory Reuse / DRY Constraints

- Reuse:
  - `_condamad/stories/regression-guardrails.md` pour les invariants applicables.
  - Les tests d'ownership scripts existants s'ils existent avant d'ajouter un nouveau fichier.
- Do not recreate:
  - Un second validateur equivalent sous un autre nom.
  - Une commande de compatibilite qui preserve le chemin racine.
- Shared abstraction allowed only if:
  - Un garde d'ownership scripts existe deja et peut recevoir une ligne exacte sans generalisation speculative.

## 10. No Legacy / Forbidden Paths

Forbidden unless explicitly approved:

- compatibility wrappers
- transitional aliases
- legacy imports
- duplicate active implementations
- silent fallback behavior
- root-level service when canonical namespace exists
- preserving old path through re-export

Specific forbidden symbols / paths:

- `scripts/validate_route_removal_audit.py`
- `validate_route_removal_audit.py` as an active root script

## 11. Removal Classification Rules

Classification must be deterministic:

- `canonical-active`: item is referenced by first-party production code or is the canonical owner.
- `external-active`: item is referenced by public docs, email templates, generated links, clients, or audit evidence.
- `historical-facade`: item delegates to a canonical implementation only to preserve an old surface.
- `dead`: item has zero references in production code, tests, docs, generated contracts, and known external surfaces.
- `needs-user-decision`: ambiguity remains after required scans and must block deletion.

Classification decision matrix:

| Classification | Allowed decisions | Rule |
|---|---|---|
| `canonical-active` | `keep` | Must not be deleted. |
| `external-active` | `keep`, `needs-user-decision` | Must not be deleted without explicit user decision. |
| `historical-facade` | `delete`, `needs-user-decision` | Must be deleted when no external blocker remains. Must not be repointed. |
| `dead` | `delete` | Must be deleted. |
| `needs-user-decision` | `needs-user-decision` | Must block implementation until decision. |

## 12. Removal Audit Format

Required audit table:

| Item | Type | Classification | Consumers | Canonical replacement | Decision | Proof | Risk |
|---|---|---|---|---|---|---|---|

Allowed decisions:

- `keep`
- `delete`
- `replace-consumer`
- `needs-user-decision`

Audit output path:

- `_condamad/stories/remove-root-route-removal-audit-validator/removal-audit.md`

## 13. Canonical Ownership

| Responsibility | Canonical owner | Non-canonical surfaces |
|---|---|---|
| Validation historique CONDAMAD | `_condamad/stories/remove-historical-facade-routes/` | `scripts/validate_route_removal_audit.py` |

## 14. Delete-Only Rule

Items classified as removable must be deleted, not repointed.

Forbidden:

- redirecting to the canonical endpoint
- preserving a wrapper
- adding a compatibility alias
- keeping a deprecated route active
- preserving the old path through re-export
- replacing deletion with soft-disable behavior

## 15. External Usage Blocker

If an item is classified as `external-active`, it must not be deleted. The dev agent must stop or record an explicit user decision with external evidence and deletion risk.

## 17. Generated Contract Check

- Generated contract check: not applicable
- Reason: no generated API, route manifest, schema, public contract, or generated client is affected.

## 18. Files to Inspect First

Codex must inspect before editing:

- `scripts/validate_route_removal_audit.py`
- `_condamad/stories/remove-historical-facade-routes/00-story.md`
- `_condamad/stories/remove-historical-facade-routes/route-consumption-audit.md`
- `_condamad/stories/regression-guardrails.md`

## 19. Expected Files to Modify

Likely files:

- `scripts/validate_route_removal_audit.py` - expected deletion if classification is `dead`.
- `_condamad/stories/remove-historical-facade-routes/00-story.md` - expected historical command reference correction.
- `_condamad/stories/remove-root-route-removal-audit-validator/removal-audit.md` - expected persisted audit.

Likely tests:

- `backend/app/tests/unit/test_scripts_ownership.py` - expected guard extension or assumption risk: unknown until repo inspection if the canonical guard has another name.

Files not expected to change:

- `backend/app/main.py` - no API route behavior is in scope.
- `frontend/src/App.tsx` - no frontend route or UI behavior is in scope.

## 20. Dependency Policy

- New dependencies: none
- Dependency changes allowed only if explicitly listed here with justification.

## 21. Validation Plan

Run or justify why skipped:

```powershell
.\.venv\Scripts\Activate.ps1
rg -n "validate_route_removal_audit.py|validate_route_removal_audit" .
pytest -q app/tests/unit/test_scripts_ownership.py
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_validate.py _condamad/stories/remove-root-route-removal-audit-validator/00-story.md
python -B .agents/skills/condamad-story-writer/scripts/condamad_story_lint.py --strict _condamad/stories/remove-root-route-removal-audit-validator/00-story.md
```

## 22. Regression Risks

- Risk: supprimer un outil encore necessaire a une preuve historique.
  - Guardrail: audit de consommation et blocker `external-active`.
- Risk: reintroduire un validateur story-specifique dans la racine `scripts/`.
  - Guardrail: test d'ownership et scan cible du chemin interdit.

## 23. Dev Agent Instructions

- Implement only this story.
- Do not broaden the domain.
- Do not introduce new dependencies unless explicitly listed.
- Do not mark a task complete without validation evidence.
- If an AC cannot be satisfied, stop and record the blocker.
- Do not preserve legacy behavior for convenience.
- Do not bypass deletion through repointing, soft-disable, wrapper, alias, fallback, or re-export.
- Ne pas traiter `stripe-listen-webhook.sh` dans cette story.

## 24. References

- `_condamad/audits/scripts-ops/2026-05-02-1847/03-story-candidates.md` - source candidate `SC-001`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/02-finding-register.md` - details du finding `F-001`.
- `_condamad/audits/scripts-ops/2026-05-02-1847/01-evidence-log.md` - preuves `E-003` et `E-014`.
- `_condamad/stories/regression-guardrails.md` - invariants transverses consultes.
