# Acceptance Traceability

| AC | Requirement | Implementation evidence | Validation evidence | Status |
|---|---|---|---|---|
| AC1 | One canonical snapshot owner exists. | `LlmReplaySnapshotModel` remains the only `__tablename__ = "llm_replay_snapshots"` owner; `log_call` instantiates only that model. | `python -B -m pytest -q tests\unit\test_replay_snapshot_v1_ownership.py --tb=short`; `rg -n 'class LlmReplaySnapshotModel|__tablename__ = "llm_replay_snapshots"' backend\app\infra\db\models\llm`. | PASS |
| AC2 | Replay snapshot schema stores approved fields. | Model and migration add `snapshot_type`, `created_at`, `input_ref`, `input_hash`, `version_identity`, `provenance`, `redaction_state`, optional `payload_enc`; metadata builder stores refs/hashes/versions only. | `python -B -m pytest -q tests\unit\test_replay_snapshot_v1_storage.py --tb=short`; model column inspection. | PASS |
| AC3 | Retention is exactly 30 days. | `log_call` sets `expires_at = created_at + timedelta(days=30)` and model default is 30 days. | `python -B -m pytest -q tests\unit\test_replay_snapshot_v1_retention.py --tb=short`. | PASS |
| AC4 | Forbidden raw fields are not persisted. | Replay metadata uses `sanitize_payload(..., Sink.LLM_REPLAY_SNAPSHOTS)` plus key/value hashes; secrets are removed before encrypted replay payload storage. | `python -B -m pytest -q tests\unit\test_replay_snapshot_v1_redaction.py --tb=short`; scoped storage scan has no forbidden matches. | PASS |
| AC5 | DB scan excludes raw data. | Integration test persists a snapshot and inspects JSON/storage columns for absence of raw prompt, birth data, identifier and secret values. | `python -B -m pytest -q tests\integration\test_replay_snapshot_v1_db_redaction.py --tb=short`. | PASS |
| AC6 | Migration matches model metadata. | Alembic head `20260525_0140` adds missing approved columns to `llm_replay_snapshots`; invariant test compares DB columns and model metadata. | `python -B -m pytest -q tests\integration\test_llm_db_invariants.py --tb=short`; `python -B -m alembic heads` => `20260525_0140 (head)`. | PASS |
| AC7 | Expired snapshots are purged safely. | Existing `purge_expired_logs` still deletes snapshots by snapshot `expires_at` before call logs, without deleting active call logs. | `python -B -m pytest -q tests\unit\test_replay_snapshot_v1_purge.py --tb=short`. | PASS |
| AC8 | Public API exposure is unchanged. | No API/router/frontend files changed; runtime guards assert no route or OpenAPI path exposes `replay_snapshot_v1`. | `python -B -c "from app.main import app; assert 'replay_snapshot_v1' not in str(app.openapi())"`; `python -B -c "from app.main import app; assert 'replay_snapshot_v1' not in {getattr(r, 'path', '') for r in app.routes}"`; `git status --short -- frontend/src` clean. | PASS |
| AC9 | Story evidence artifacts are persisted. | Evidence files under `_condamad/stories/CS-295-replay-snapshot-v1-storage-redaction/evidence/` plus generated final evidence and story status update. | `python -B .agents\skills\condamad-dev-story\scripts\condamad_validate.py _condamad\stories\CS-295-replay-snapshot-v1-storage-redaction`; final evidence file presence checks. | PASS |

