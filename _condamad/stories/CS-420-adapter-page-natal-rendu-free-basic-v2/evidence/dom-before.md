# DOM Before - CS-420

Dedicated browser DOM was not captured before implementation because the capsule generated files were missing and were repaired first.

Baseline used for implementation:
- Story current-state evidence states `/natal` classified a valid free short payload as a complete reading to regenerate.
- Existing tests before this change asserted that free public sections were not rendered and complete legacy payloads showed the regeneration message.

The before/after behavioral delta is proven by updated Vitest fixtures and the final DOM guard evidence.
