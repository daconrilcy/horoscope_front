# Prediction Runtime Consumption After

- `NatalChart.houses` carries `HouseRuntimeData`.
- `DomainRouter` reads runtime house numbers from event metadata.
- `NatalSensitivityCalculator` reads runtime occupants, rulers and house kinds.
