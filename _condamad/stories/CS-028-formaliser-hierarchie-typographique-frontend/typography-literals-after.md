<!-- Inventaire typographique final apres formalisation CS-028. -->

# Typography Literals After

Ajouts:

- Tokens `--type-*` dans `frontend/src/styles/design-tokens.css`.
- Classes `.type-*` dans `frontend/src/styles/utilities.css`.
- Registre `frontend/src/styles/typography-roles.md`.

Guard: `npm run test -- design-system`.

Resultat: les roles requis `page-title`, `section-title`, `card-title`, `body`,
`body-muted`, `metadata`, `label`, `eyebrow`, `cta` et `numeric` sont verifies.
