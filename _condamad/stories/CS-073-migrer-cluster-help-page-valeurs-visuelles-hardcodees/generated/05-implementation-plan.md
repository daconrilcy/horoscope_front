# Implementation Plan — CS-073

1. Capture before hardcoded-value evidence for the bounded HelpPage cluster.
2. Register local `--help-*` semantic variables for repeated visual decisions.
3. Replace main-cluster literals with existing tokens or local semantic owners.
4. Preserve visual scale and runtime CSS scope discovered during review.
5. Add executable anti-return guard for migrated literals.
6. Run focused frontend tests, lint, scans and story validation.
