# Acceptance Traceability

| AC | Requirement | Code evidence | Validation evidence | Status |
|---|---|---|---|---|
| AC1 | All required fact families are emitted. | `natal_fact_graph.py`; `natal_fact_graph_builder.py` emits all `NatalFactFamily` values; rich fixture now covers `asc` and `mc`. | Targeted pytest PASS: `19 passed`. | PASS |
| AC2 | Every fact has traceable source paths. | `NatalFact.source_paths` is required and validated; builder attaches runtime source paths per family. | `test_fact_graph_emits_required_families_and_traceable_contract` PASS. | PASS |
| AC3 | Fact identifiers are deterministic. | `_stable_id()` derives ids from stable family/object parts and graph id from ordered fact ids. | `test_fact_ids_are_deterministic_and_major_aspects_keep_pair_identity` PASS. | PASS |
| AC4 | Date-only input gates time-dependent facts. | Builder gates angle, house and rulership families through `EligibilityContext`. | `test_date_only_gates_time_dependent_facts` PASS. | PASS |
| AC5 | Non-time-dependent families remain available. | Date-only path keeps luminary, planet position, sign, node and major aspect facts. | `test_date_only_keeps_non_time_dependent_families` PASS. | PASS |
| AC6 | Major aspects keep pair identity. | Aspect facts sort the participant pair and include the aspect code in objects/source paths. | `test_fact_ids_are_deterministic_and_major_aspects_keep_pair_identity` PASS. | PASS |
| AC7 | Runtime projections are the only calculation source. | Builder consumes `ChartInterpretationInputRuntimeData` plus `EligibilityContext`; no calculator/import owner added. | AST guard PASS in `test_fact_graph_builder_uses_runtime_inputs_without_local_recalculation` and `test_natal_fact_graph_builder_reuses_runtime_sources_without_local_calculation`; recalculation `rg` PASS no matches on active fact graph modules/tests. | PASS |
| AC8 | Internal facts stay separate from editorial candidates. | `to_internal_payload()` includes `source_paths`; `to_editorial_candidate_payload()` omits them and filters `editorial_candidate`. | `test_internal_sources_do_not_leak_to_editorial_candidates` PASS; `rg -n "source_paths" backend\app\api frontend\src ...` PASS no matches. | PASS |
| AC9 | Fact graph emits no final user text. | New model/builder expose structured fact fields only. | Forbidden prose scan PASS no matches; exact command recorded in final evidence. | PASS |
| AC10 | Story evidence artifacts are persisted. | `generated/03-acceptance-traceability.md`, `generated/10-final-evidence.md`, `evidence/fact-graph-before.md`, `evidence/fact-graph-after.md`, `evidence/validation.txt`, `evidence/recalculation-guards.txt`, `generated/09-dev-log.md`. | `condamad_story_validate.py`, `condamad_story_lint.py --strict` and `condamad_validate.py <capsule> --final` PASS after evidence update. | PASS |
