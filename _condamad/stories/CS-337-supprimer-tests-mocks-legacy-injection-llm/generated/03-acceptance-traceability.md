# Acceptance Traceability

| AC | Requirement | Implementation evidence | Validation evidence | Status |
|---|---|---|---|---|
| AC1 | Every old-key test hit is classified. | `_condamad/stories/CS-337-supprimer-tests-mocks-legacy-injection-llm/evidence/test-cleanup-audit.md` classifies deleted legacy natal surfaces, adapted modern surfaces, retained negative guards and residual non-LLM/external owners. | `evidence/test-legacy-scan-before.txt`; `evidence/test-legacy-scan-after.txt`; targeted `rg` for deleted symbols returned no matches. | PASS |
| AC2 | Old LLM compatibility tests are deleted. | Deleted `backend/app/tests/unit/legacy_services/test_natal_interpretation_service_v2_refacto.py` and `backend/app/tests/unit/legacy_services/legacy_natal_interpretation_service.py`; golden natal fixtures now use `llm_astrology_input_v1`. | `python -B -m pytest -q tests --tb=short` -> 1208 passed, 218 deselected; `python -B -m pytest -q app\tests\unit\legacy_services app\tests\integration\test_admin_llm_natal_prompts.py --tb=short` -> 5 passed, 6 deselected. | PASS |
| AC3 | Old fallback mocks are absent. | Removed the natal legacy helper path and replaced old natal handoff data in gateway/golden tests with the canonical input. Residual fallback hits are governance/provider/non-natal owners in the audit. | `evidence/test-legacy-scan-after.txt`; `pytest` targeted orchestration suite -> 41 passed, 8 deselected. | PASS_WITH_LIMITATIONS |
| AC4 | Modern payload tests cover `llm_astrology_input_v1`. | Updated `test_llm_execution_request.py`, `test_llm_gateway_compose.py`, golden fixtures and golden regression assertions to use `llm_astrology_input_v1`. Existing boundary tests remain canonical. | `python -B -m pytest -q tests\llm_orchestration\test_llm_astrology_input_boundaries.py ...` included in targeted run -> 41 passed, 8 deselected. | PASS |
| AC5 | Negative old-key guards remain. | Kept `backend/tests/architecture/test_llm_legacy_extinction.py` and `backend/tests/llm_orchestration/test_llm_astrology_input_boundaries.py` old-key negative assertions. | `python -B -m pytest -q tests\architecture\test_llm_legacy_extinction.py` included in targeted run -> PASS. | PASS |
| AC6 | No opportunistic skip remains. | No skip/xfail was added; residual skip/xfail hits in scans are pre-existing unrelated tests and are classified as residual non-story owners. | `evidence/test-legacy-scan-after.txt`; `git diff --check` PASS. | PASS_WITH_LIMITATIONS |
| AC7 | Public API surface stays unchanged. | Only backend tests/fixtures plus golden replay context routing changed; no API router touched. `openapi-before.json` equals `openapi-after.json`. | `python -B -c "from app.main import app; assert app.routes; assert app.openapi()['paths']"` PASS; OpenAPI string checks PASS; `TestClient(app).get('/openapi.json')` PASS. | PASS |
| AC8 | Removal evidence is persisted. | Before/after scans, OpenAPI snapshots, validation output and cleanup audit are persisted under `evidence/`. | `python -B -c "from pathlib import Path; assert Path('../_condamad/stories/CS-337-supprimer-tests-mocks-legacy-injection-llm/evidence/validation.txt').exists()"` PASS via final validation file presence. | PASS |

Status values: `PENDING`, `PASS`, `PASS_WITH_LIMITATIONS`, `FAIL`, `BLOCKED`.
