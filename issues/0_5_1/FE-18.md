---
title: FE-18 — Admin dev: clear price_lookup cache
labels: frontend, billing, stripe, priority:high
assignees: 
  - 
created: 2025-11-03
---

# FE-18 — Admin dev: clear price_lookup cache

## Contexte

Action dev-only pour vider le cache price_lookup via endpoint admin (avec X-Admin-Key).

## Objectif

- Mettre en œuvre admin dev: clear price_lookup cache pour aligner le front sur l'API backend Stripe.
- Offrir une UX claire, résiliente et testable.

## Portée / Hors-périmètre

- **Dans le scope** : Admin dev: clear price_lookup cache.
- **Hors scope** : pricing logic côté backend, routes Stripe Server/webhooks (déjà gérées au backend).

## Design & Implémentation

- Dossier(s) impactés: `src/services/billing/*`, `src/components/billing/*`, `src/pages/billing/*`, `src/routes/*` (à adapter).
- Tech: React + TS, Zustand (si store), Zod pour validation, MSW pour tests.

### Tâches de dev

- [ ] Ajouter bouton “Vider cache price_lookup” (visible en dev ou rôle admin local).
- [ ] Appel `/v1/billing/admin/cache/clear` avec header `X-Admin-Key` (saisi via input dev-only).
- [ ] Toast succès/erreur.

### Accessibilité & i18n

- Labels et descriptions accessibles (ARIA si modal).
- Texte FR prêt pour i18n.

### Observabilité

- Logger `request_id` (si header) dans un EventBus local (dev-only) pour corrélation.

## Tests

**MSW**

- [ ] 200 → toast succès.
- [ ] 403/401 → message d’erreur explicite.

**Unit**

- [ ] Masquage UI en prod.

## Critères d’acceptation (AC)

- Le bouton n’apparaît qu’en dev/admin.
- Le cache est vidé et feedback affiché.

## Sécurité

- Idempotency-Key générée côté front pour toutes mutations.
- Ne jamais exposer de clés secrètes Stripe (utiliser les endpoints backend).

## Risques / Mitigations

- Erreurs de whitelist d’URL retour: message explicite + fallback configuré.
- Flags désactivés: champs masqués proprement.

## PR attendue

**Title**: feat(front-admin): clear price_lookup cache (dev only)  
**Body** (exemple à adapter) :

```
feat(front-admin): clear price_lookup cache (dev only)

Contexte
- Implémente FE-18: Admin dev: clear price_lookup cache.
- Aligne le front avec les endpoints backend Stripe.

Changements principaux
- [x] Composants/Service
- [x] Validation Zod
- [x] Feature flags respectés

Tests
- Unitaires (Vitest)
- MSW
- E2E (Playwright)

Docs
- README front mis à jour si applicable

Closes: <numéro de l'issue>
```

**Checklist PR**

- [ ] Lint/Typecheck ok
- [ ] Tests unitaires ok
- [ ] E2E scénarios clés ok (local)
- [ ] Docs mises à jour
- [ ] Pas d’infos sensibles dans les logs

## Références

- PRICE_LOOKUP_README.md (admin/cache)
