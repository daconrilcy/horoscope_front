# Rétrospective Epic 30 — LLM Gateway GPT-5 & Structured Outputs

**Date :** 2026-03-04  
**Epic :** 30 — Montée en gamme LLM (Gateway GPT-5, schémas structurés, fiabilité runtime)  
**Facilité par :** Bob (Scrum Master)  
**Participants :** Cyril (Project Lead), Alice (PO), Charlie (Senior Dev), Dana (QA), Elena (Junior Dev)

---

## Résumé

### Métriques

| Métrique | Valeur |
|---|---|
| Stories livrées | 8/8 |
| Story finale validée | 30-8 |
| Validation backend ciblée | 62/62 tests passants |
| Vérification runtime | short + complete + cas erreur validés |
| Régressions critiques ouvertes | 0 |

### Objectifs atteints

- Routage `natal_interpretation` aligné sur `AstroResponse_v3` côté seed/use-cases.
- Prompt GPT-5 v3 publié avec idempotence (re-run seed sans casse).
- Réponse `complete` stable en schéma v3.
- Disclaimers conservés en couche applicative API, exclus du payload LLM persisté v3.
- Frontend rendu résilient quand `interpretation.disclaimers` est absent (fallback top-level).
- Bouton de régénération disponible en `short` et `complete`, avec refresh répétable côté UI.
- Prompt v3 enrichi avec des modules thématiques `NATAL_*` pour des lectures orientées usage
  (profil psycho, shadow integration, leadership, créativité, relationnel, collectif, valeurs,
  évolution avec erreur dédiée si données manquantes).
- Stabilisation post-release: correction du crash runtime sur doublons cache (`MultipleResultsFound`)
  + migration DB de déduplication et contraintes d'unicité partielles.
- Industrialisation UX livrée: disclaimer légal en footer dédié, panneau audit `evidence` pliable,
  dédupliqué et regroupé par catégories.

---

## Ce qui a bien marché

1. Diagnostic en conditions réelles
- Les checks via `curl` ont mis en évidence les écarts runtime que les tests unitaires ne capturaient pas.

2. Isolation de validation
- L’usage de DB dédiées de validation a supprimé les faux positifs liés aux effets de bord des suites de tests.

3. Correctifs ciblés, delta minimal
- Le correctif front a été limité au contrat de données et au rendu défensif, sans refactor global.

4. Rigueur seed/runtime
- Le verrouillage de l’ordre de seed (`use_cases -> chapter 29 -> story 30-8 -> personas`) a rétabli la cohérence.

---

## Points d’attention

1. Couplage implicite entre scripts de seed
- Un seed générique pouvait écraser une configuration v3 sans garde explicite.

2. Écart contrat API vs contrat UI
- `disclaimers` déplacé côté API mais encore attendu dans certains chemins UI.

3. Validation “tests only” insuffisante
- Certaines anomalies n’apparaissaient qu’en exécution réelle complète.

---

## Actions retenues

1. Standardiser les garde-fous de seed
- Chaque seed qui met à jour `output_schema_id` doit vérifier la version actuelle avant overwrite.

2. Check de cohérence post-seed obligatoire
- Ajouter une vérification automatisée `use_case -> schema` pour les use-cases critiques.

3. Contract-tests front/back ciblés story
- Pour chaque story critique, ajouter un test qui valide la forme JSON réelle (champ top-level vs nested).

4. Non-régression UX de refresh
- Pour les flows premium, ajouter un test UI garantissant qu'un clic "Nouvelle interprétation" relance bien une requête forcée à chaque tentative.

5. Smoke runtime systématique avant clôture
- Exiger un run `curl` de parcours nominal + erreurs métier sur endpoints critiques.

6. Prévention data-integrity sur tables de cache métier
- Toute table exploitée avec `scalar_one_or_none()` doit avoir une contrainte d'unicité explicite (ou lecture déterministe multi-row).

7. Runbook migration sécurisé pré-prod
- Exécuter systématiquement un script de diagnostic des doublons avant migration destructive.

8. Gouvernance UX/legal
- Séparer strictement les blocs "traçabilité" (audit evidence) et "mentions légales" (footer applicatif).

---

## Addendum post-rétro (même journée)

- Hotfix appliqué après observation terrain:
  - fallback applicatif en cas de doublons historiques.
  - migration `20260304_0028` exécutée en local avec succès.
  - script d'audit prêt pour staging/prod.
- Non-régression éditoriale:
  - prompt v3 renforcé (lisibilité, anti-redondance, observables concrets, leviers actionnables).
  - tests de contrat prompt/lint ajoutés et passants.
- Non-régression frontend:
  - tests UI ajustés pour panneau audit pliable + labels i18n.
  - libellé Maison VI revu pour réduire le risque de perception médicale.

## Addendum post-rétro (2026-03-05)

- Story 30-9 finalisée côté export PDF templatisable avec hardening production:
  - pagination explicite par blocs (head/paragraph) et suppression du double moteur de pagination,
  - calibration runtime via config template (`page_budget_lines`, `section_head_extra_lines`,
    `paragraph_spacing_lines`, `section_tail_spacing_lines`, `sections_start_new_page_min_remaining_lines`),
  - mode `sections_start_new_page` passé en "best effort" basé sur le reste réel après intro,
  - nettoyage des artefacts JSON résiduels en fin de contenu et chunking robuste des longs tokens.
- Gouvernance templates:
  - création des profils `prod_premium` (default) et `prod_compact`,
  - script d'upsert idempotent `seed_pdf_templates_prod.py`,
  - reset des defaults limité au scope seedé (pas de wipe global table).

---

## Décision de clôture

Epic 30 est clôturé avec rétro réalisée.  
La story 30-8 est considérée stable en conditions réelles sur le périmètre validé (backend + frontend interpretation flow).
