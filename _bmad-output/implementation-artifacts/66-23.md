# Story 66.23: Normalisation canonique des identifiants de feature sur le domaine natal

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Platform Architect,
I want to define and enforce a single canonical feature identifier for the natal domain across runtime, admin, seeds, coverage, and observability,
so that prompt assembly resolution, execution profile lookup, publication validation, seeds, tests, and telemetry all rely on the same taxonomy and no residual naming drift remains between `natal` and `natal_interpretation`.

## Contexte

Le chemin nominal natal doit utiliser `feature="natal"` et une variation métier portée par `subfeature`, puis suivre le pipeline standard `feature/subfeature/plan`.

Les stories 66.15, 66.20, 66.21 et 66.22 ont déjà convergé vers ce modèle, mais le code et les artefacts contiennent encore des usages historiques de `natal_interpretation`. Certains sont légitimes comme `use_case` contractuel ou nom de service/fichier, mais aucun ne doit rester une valeur nominale de `feature`.

Règle canonique cible :

- domaine fonctionnel : `feature="natal"`
- variation métier intra-domaine : `subfeature`
- variation contractuelle forte : `use_case` distinct seulement si le schéma de sortie change réellement
- compatibilité historique : alias/mapping explicitement déprécié, non nominal, télémétré, jamais source primaire

Phrase documentaire obligatoire à intégrer dans `docs/llm-prompt-generation-by-feature.md` :

> La feature canonique du domaine natal est `natal`. La désignation `natal_interpretation` ne doit plus être utilisée comme identifiant de feature nominal ; les variations métier relèvent du `subfeature` ou du `use_case` selon la nature de l'écart.

## Acceptance Criteria

1. **AC1 - Canonical feature identifier** : Le domaine natal utilise `feature="natal"` comme unique identifiant de feature canonique sur tout chemin nominal runtime, admin, publication, seed et observabilité.
2. **AC2 - Elimination of nominal `natal_interpretation` as feature** : Aucun appel nominal, aucune assembly publiée, aucune configuration nominale publiée ou publiable, aucune fixture nominale, aucun seed nominal, aucune métrique nominale et aucun exemple documentaire canonique ne doivent utiliser `feature="natal_interpretation"`.
3. **AC3 - Resolution consistency** : Toute résolution assembly ou waterfall concernant le domaine natal part de `feature="natal"` puis d'un `subfeature` canonique. Il ne doit plus exister de divergence de lookup entre un chemin résolu en `natal` et un autre résolu en `natal_interpretation`.
4. **AC4 - Compatibility governance** : Si un alias historique `natal_interpretation -> natal` est conservé temporairement, il est centralisé, classé non nominal, télémétré, interdit comme source de publication nominale, ne sert jamais de clé primaire de résolution nominale dans les registries, et est documenté avec trajectoire d'extinction.
5. **AC5 - Admin and publication lock** : Les validations d'admin et de publication refusent toute nouvelle configuration nominale utilisant `feature="natal_interpretation"` comme identifiant primaire de feature.
6. **AC6 - Seeds and fixtures normalization** : Tous les seeds, fixtures, factories, jeux d'essai et données de couverture relevant du domaine natal sont normalisés vers `feature="natal"`, sauf tests explicitement marqués comme compatibilité legacy.
7. **AC7 - Observability normalization** : Les métriques, logs structurés, labels et dashboards ne fragmentent plus la lecture nominale entre `natal` et `natal_interpretation`. Les événements nominaux remontent sous `feature="natal"` uniquement.
8. **AC8 - Test coverage** : La suite couvre la résolution nominale `feature="natal"`, le rejet des configurations nominales `feature="natal_interpretation"`, le comportement de compatibilité si alias conservé, et l'unicité des labels d'observabilité côté natal.
9. **AC9 - Documentation update** : `docs/llm-prompt-generation-by-feature.md` explicite la décision canonique, supprime les formulations ambiguës, documente le statut de tout alias legacy résiduel, et garantit que tous les exemples nominaux natal utilisent `feature="natal"`.
10. **AC10 - No silent drift** : Aucune couche ne réécrit ni ne tolère silencieusement `natal_interpretation` comme feature nominale sans traçabilité explicite.

## Tasks / Subtasks

- [x] Task 1: Introduire la source de vérité taxonomique natal (AC1, AC3, AC10)
  - [x] Créer ou compléter un module central de taxonomie LLM, par exemple `backend/app/llm_orchestration/feature_taxonomy.py`, avec `NATAL_CANONICAL_FEATURE = "natal"` et la liste des features dépréciées/interdites en nominal.
  - [x] Y déclarer aussi la liste canonique des `subfeature` autorisées pour le domaine natal afin que la variation métier ne reste pas redéclarée localement.
  - [x] Exposer des helpers explicites du type `normalize_legacy_feature(...)`, `is_nominal_feature_allowed(...)` et `assert_nominal_feature_allowed(...)`.
  - [x] Éviter toute nouvelle allowlist locale dans le gateway, l'admin, les seeds ou les tests.

- [x] Task 2: Durcir runtime et resolvers (AC1, AC2, AC3, AC4, AC10)
  - [x] Appliquer le contrôle avant la résolution principale dans `LLMGateway.execute_request()`.
  - [x] Garantir que `AssemblyRegistry`, `AssemblyResolver` et `ExecutionProfileRegistry` ne cherchent jamais nominalement `feature="natal_interpretation"` pour le domaine natal.
  - [x] Si un alias legacy est conservé, le remapping doit être exécuté avant lookup, marquer le chemin `is_nominal=false`, et émettre un signal d'observabilité.
  - [x] Vérifier que `DEPRECATED_USE_CASE_MAPPING` reste une compatibilité de `use_case`, pas une source primaire de `feature`.

- [x] Task 3: Verrouiller admin, publication et profils d'exécution (AC2, AC5)
  - [x] Rejeter `feature="natal_interpretation"` dans `PromptAssemblyConfig` côté modèle Pydantic/admin pour toute configuration nominale publiable ; un brouillon non publiable ne peut être toléré que s'il est explicitement marqué legacy/migration et bloqué avant publication.
  - [x] Rejeter la publication ou rollback d'une assembly nominale utilisant cette feature.
  - [x] Rejeter la publication d'un `LlmExecutionProfileModel` avec `feature="natal_interpretation"`.
  - [x] Retourner des erreurs explicites orientées correction : utiliser `feature="natal"` et un `subfeature` métier canonique.

- [x] Task 4: Migrer seeds, fixtures et matrices de couverture (AC2, AC6, AC8)
  - [x] Auditer `backend/app/llm_orchestration/seeds/`, `backend/scripts/`, `backend/tests/evaluation/` et les tests d'intégration LLM.
  - [x] Remplacer tout `feature="natal_interpretation"` nominal par `feature="natal"`.
  - [x] Formaliser la taxonomie cible des `subfeature` natal dans la source de vérité : `natal_interpretation` ne doit être conservé comme `subfeature` nominal que si la story le classe explicitement comme variante métier canonique ; sinon migrer vers des variantes non préfixées du type `interpretation`, `short` ou `full`.
  - [x] Appliquer cette décision aux couples actuellement attendus, notamment `natal/natal_interpretation/free|premium`, sans laisser le choix à chaque call site.
  - [x] Les tests legacy doivent être nommés et commentés comme tels ; ils ne doivent pas être confondus avec fixtures nominales.

- [x] Task 5: Normaliser l'observabilité (AC4, AC7, AC10)
  - [x] S'assurer que `GatewayMeta.feature`, `LlmCallLogModel.feature`, `llm_gateway_requests_total`, `llm_gateway_fallback_usage_total` et `llm_governance_event_total` reportent `feature="natal"` pour les chemins nominaux natal.
  - [x] Ajouter un événement dédié ou un label explicite pour toute compatibilité alias, par exemple `event_type=legacy_feature_alias_used` ou `fallback_type=deprecated_feature_alias`.
  - [x] Normaliser aussi les dimensions de dashboards, filtres, rapports et agrégations historiques afin de ne pas conserver deux buckets nominaux concurrents `natal` et `natal_interpretation` côté lecture.

- [x] Task 6: Mettre à jour la documentation d'architecture (AC4, AC9)
  - [x] Mettre à jour `docs/llm-prompt-generation-by-feature.md` avec la décision canonique et la phrase de verrou sémantique.
  - [x] Supprimer ou requalifier les formulations ambiguës autour de `natal_interpretation`.
  - [x] Documenter tout alias résiduel avec statut, périmètre, métrique et condition d'extinction.
  - [x] Garantir que les exemples nominaux natal utilisent `feature="natal"`.

## Dev Notes

### Points d'attention

- Ne pas renommer aveuglément tous les symboles `natal_interpretation` : plusieurs occurrences désignent un `use_case`, un service, un router, une table ou un champ métier historique. La story interdit `natal_interpretation` comme **feature nominale**, pas nécessairement comme nom de fichier ou contrat existant.
- La normalisation de `feature` ne vaut pas renommage automatique des `subfeature`. Les `subfeature` natal doivent être traités explicitement selon la taxonomie cible décidée par cette story, puis appliqués de façon uniforme.
- La compatibilité existante `DEPRECATED_USE_CASE_MAPPING` dans `backend/app/prompts/catalog.py` mappe déjà les use cases historiques nataux vers `feature="natal"`. Si un alias de feature est ajouté, il doit être séparé de ce mapping pour éviter de mélanger compatibilité de `use_case` et compatibilité de `feature`.
- Le fallback `use_case-first` est déjà gouverné par 66.21 ; ne pas le réactiver comme solution rapide pour contourner la taxonomie.
- Les validations 66.22 sur les providers montrent le pattern attendu : une source de vérité centrale consommée par admin, publication et runtime, avec métrique de gouvernance.

### Code et artefacts à inspecter en priorité

- `backend/app/llm_orchestration/gateway.py` : application de `DEPRECATED_USE_CASE_MAPPING`, résolution assembly, plan d'exécution et métadonnées.
- `backend/app/llm_orchestration/services/assembly_registry.py` : lookup des assemblies publiées.
- `backend/app/llm_orchestration/services/assembly_resolver.py` : preview, placeholders et target `feature/subfeature/plan`.
- `backend/app/llm_orchestration/services/execution_profile_registry.py` : waterfall `feature + subfeature + plan`.
- `backend/app/llm_orchestration/admin_models.py` et `backend/app/llm_orchestration/services/assembly_admin_service.py` : payload admin, création draft, publication, rollback.
- `backend/app/infra/db/models/llm_assembly.py` et `backend/app/infra/db/models/llm_execution_profile.py` : contraintes DB et validations de publication.
- `backend/app/llm_orchestration/services/observability_service.py` et `backend/app/infra/db/models/llm_observability.py` : logs, labels et stockage.
- `backend/app/services/ai_engine_adapter.py` : `generate_natal_interpretation()` doit rester l'entrée applicative canonique et produire `feature="natal"`.
- `backend/app/llm_orchestration/seeds/seed_66_20_taxonomy.py`, `backend/scripts/seed_66_20_convergence.py`, `backend/scripts/seed_66_15_assembly_convergence.py`, `backend/app/llm_orchestration/seeds/seed_assembly.py` : seeds et migrations à normaliser.
- `backend/tests/evaluation/evaluation_matrix.yaml` et `backend/app/llm_orchestration/eval_fixtures/` : buckets de couverture et fixtures.
- `docs/llm-prompt-generation-by-feature.md` : doctrine officielle à verrouiller.

### Testing Requirements

- Ajouter une suite dédiée, par exemple `backend/tests/integration/test_story_66_23_natal_feature_taxonomy.py`.
- Couvrir :
  - résolution nominale `feature="natal"` avec un `subfeature` valide vers assembly et `ExecutionProfile`;
  - rejet d'une `PromptAssemblyConfig` ou publication avec `feature="natal_interpretation"`;
  - rejet d'un `LlmExecutionProfileModel` publié avec `feature="natal_interpretation"`;
  - waterfall `ExecutionProfileRegistry` : un profil publié sous `feature="natal"` est bien trouvé pour un appel natal nominal, et aucun lookup nominal secondaire en `natal_interpretation` n'est effectué;
  - compatibilité legacy si conservée : remapping vers `natal`, `is_nominal=false`, métrique ou log explicite;
  - logs/métriques nominaux natal avec un seul label `feature="natal"`;
  - seed integrity : aucun seed nominal actif ne référence `feature="natal_interpretation"`;
  - absence de double bucket dans `backend/tests/evaluation/evaluation_matrix.yaml`.
- Commandes obligatoires côté backend, toujours après activation du venv PowerShell :
  - `.\.venv\Scripts\Activate.ps1`
  - `cd backend`
  - `ruff format .`
  - `ruff check .`
  - `pytest -q`

### Project Structure Notes

- Respecter la stack existante : backend FastAPI/Python, dépendances dans `backend/pyproject.toml`, pas de `requirements.txt`.
- Conserver le plus petit delta cohérent : privilégier un module de taxonomie partagé et des appels de validation courts plutôt que des règles recopiées dans chaque couche.
- Pas de migration DB obligatoire attendue sauf si une contrainte persistée existante rend impossible le verrou de publication. Si une migration est ajoutée, elle doit être idempotente et ne modifier que les champs strictement nécessaires.

### Previous Story Intelligence

- **66.21** a introduit la gouvernance des fallbacks via `FallbackGovernanceRegistry` et la métrique `llm_gateway_fallback_usage_total`. Réutiliser ce vocabulaire pour tout alias legacy, au lieu de créer une compatibilité muette.
- **66.22** a introduit `backend/app/llm_orchestration/supported_providers.py` comme source de vérité provider et a aligné admin, publication et gateway. Le même pattern est attendu ici pour la taxonomie feature.
- Les stories précédentes ont déjà montré que `ruff check .` global peut échouer sur des erreurs préexistantes. Le dev doit distinguer les erreurs introduites par cette story des dettes hors périmètre, sans masquer un échec sur les fichiers modifiés.

### References

- Doctrine des prompts : `docs/llm-prompt-generation-by-feature.md`
- Epic 66 : `_bmad-output/planning-artifacts/epic-66-llm-orchestration-contrats-explicites.md`
- Story précédente provider locking : `_bmad-output/implementation-artifacts/66-22.md`
- Story gouvernance fallbacks : `_bmad-output/implementation-artifacts/66-21.md`

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

### Completion Notes List

- Ajout de la source de vérité `backend/app/llm_orchestration/feature_taxonomy.py` avec feature canonique `natal`, subfeatures natales canoniques, mapping des aliases legacy et helpers de validation/normalisation.
- Durcissement des chemins admin nominaux : rejet des features legacy avant normalisation dans `PromptAssemblyTarget`, `PromptAssemblyConfig` et `LlmExecutionProfileCreate`.
- Durcissement des publications ORM : validation taxonomique sur `PromptAssemblyConfigModel` et `LlmExecutionProfileModel` lors du passage à `PUBLISHED`, afin d'empêcher toute publication via écriture directe ou rollback d'un artefact legacy.
- Durcissement runtime : normalisation explicite dans `LLMGateway.execute_request()` et `_resolve_plan()`, émission de `legacy_feature_alias_used` et tracking `DEPRECATED_FEATURE_ALIAS` quand un alias de feature est rencontré sur un chemin non nominal gouverné.
- Verrouillage des registries : `AssemblyRegistry` et `ExecutionProfileRegistry` rejettent désormais les clés legacy avant normalisation implicite, ce qui supprime le remapping silencieux comme clé primaire de lookup nominal.
- Migration de la taxonomie des subfeatures natal dans les seeds et la couverture vers `interpretation`, `short`, `psy_profile`, `shadow_integration`, `leadership_workstyle`, `creativity_joy`, `relationship_style`, `community_networks`, `values_security` et `evolution_path`.
- Mise à jour de la matrice d'évaluation pour utiliser `feature="natal"` avec `subfeature="interpretation"` sur les parcours nominaux.
- Ajustement du pipeline gateway et du renderer pour propager `required_prompt_placeholders` sur les assemblies résolues et traiter correctement les placeholders présents avec valeur `None`.
- Stabilisation suite aux code reviews : correction des régressions de convergence 66.15/66.20, ajout de tests négatifs sur les modèles admin, les registries et les publications, puis suppression des derniers écarts lint/format.
- Vérifications finales exécutées dans le venv :
  - `ruff check` ciblé sur les fichiers modifiés : OK
  - `ruff format --check` ciblé sur les fichiers modifiés : OK
  - `pytest -q app/llm_orchestration/tests/test_story_66_23_taxonomy.py app/llm_orchestration/tests/test_story_66_15_convergence.py app/llm_orchestration/tests/test_story_66_20_convergence.py app/llm_orchestration/tests/test_story_66_11_execution_profiles.py app/llm_orchestration/tests/test_story_66_12_length_budgets.py app/llm_orchestration/tests/test_story_66_14_context_quality.py app/llm_orchestration/tests/test_story_66_18_stable_profiles.py app/llm_orchestration/tests/test_gateway_pipeline.py` : `31 passed`

### File List

- `backend/app/infra/db/models/llm_assembly.py`
- `backend/app/infra/db/models/llm_execution_profile.py`
- `backend/app/llm_orchestration/admin_models.py`
- `backend/app/llm_orchestration/feature_taxonomy.py`
- `backend/app/llm_orchestration/gateway.py`
- `backend/app/llm_orchestration/models.py`
- `backend/app/llm_orchestration/seeds/seed_66_20_taxonomy.py`
- `backend/app/llm_orchestration/services/assembly_admin_service.py`
- `backend/app/llm_orchestration/services/assembly_registry.py`
- `backend/app/llm_orchestration/services/execution_profile_registry.py`
- `backend/app/llm_orchestration/services/observability_service.py`
- `backend/app/llm_orchestration/services/prompt_renderer.py`
- `backend/app/llm_orchestration/tests/test_gateway_pipeline.py`
- `backend/app/llm_orchestration/tests/test_story_66_11_execution_profiles.py`
- `backend/app/llm_orchestration/tests/test_story_66_12_length_budgets.py`
- `backend/app/llm_orchestration/tests/test_story_66_14_context_quality.py`
- `backend/app/llm_orchestration/tests/test_story_66_15_convergence.py`
- `backend/app/llm_orchestration/tests/test_story_66_18_stable_profiles.py`
- `backend/app/llm_orchestration/tests/test_story_66_23_taxonomy.py`
- `backend/scripts/seed_66_15_assembly_convergence.py`
- `backend/scripts/seed_66_20_convergence.py`
- `backend/tests/evaluation/evaluation_matrix.yaml`
- `docs/llm-prompt-generation-by-feature.md`
- `_bmad-output/implementation-artifacts/66-23.md`
